# WEB_READ_NOTES_ALL_v0.4

> Generated from arXiv/web-read evidence where available plus structured metadata for all papers in `tables/papers_master.csv`.


---

# A Mechanistic View on Video Generation as World Models: State and Dynamics

## 1. Metadata

- Year: 2026
- ID: arXiv:2601.17067
- URL: https://arxiv.org/abs/2601.17067
- Category: S0 Existing surveys / 相邻综述 
- Priority: High background
- Training-free: N/A
- Original status: Verified by web
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

A Mechanistic View on Video Generation as World Models: State and Dynamics 属于 S0 Existing surveys / 相邻综述；当前根据题名、分类表与可用网页元信息定位为：和本文的 world-state memory 论点高度相关，可引用其 state/dynamics taxonomy。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 state construction / dynamics 在 implicit context / explicit latent compression 载体上的建模、压缩、检索或评测。

## 4. Memory object

- state construction / dynamics

## 5. Memory substrate

- implicit context / explicit latent compression

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 state construction / dynamics 相关的历史状态或评测对象。 |
| Store | 主要载体：implicit context / explicit latent compression。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 和本文的 world-state memory 论点高度相关，可引用其 state/dynamics taxonomy。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S0 Existing surveys / 相邻综述 
- Role: 和本文的 world-state memory 论点高度相关，可引用其 state/dynamics taxonomy。
- Priority: High background


---

# A Survey on Long Video Generation

## 1. Metadata

- Year: 2024
- ID: arXiv:2403.16407
- URL: https://arxiv.org/abs/2403.16407
- Category: S0 Existing surveys / 相邻综述 
- Priority: Background
- Training-free: N/A
- Original status: Verified by web / 需复核最新版本
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

A Survey on Long Video Generation 属于 S0 Existing surveys / 相邻综述；当前根据题名、分类表与可用网页元信息定位为：说明已有 long-video survey 的边界：它讲长视频范式，不以 memory system 为中心。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 long-horizon temporal coherence 在 divide-and-conquer / temporal autoregression 载体上的建模、压缩、检索或评测。

## 4. Memory object

- long-horizon temporal coherence

## 5. Memory substrate

- divide-and-conquer / temporal autoregression

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 long-horizon temporal coherence 相关的历史状态或评测对象。 |
| Store | 主要载体：divide-and-conquer / temporal autoregression。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 说明已有 long-video survey 的边界：它讲长视频范式，不以 memory system 为中心。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S0 Existing surveys / 相邻综述 
- Role: 说明已有 long-video survey 的边界：它讲长视频范式，不以 memory system 为中心。
- Priority: Background


---

# A Survey on Long-Video Storytelling Generation

## 1. Metadata

- Year: 2025
- ID: arXiv:2507.07202
- URL: https://arxiv.org/abs/2507.07202
- Category: S0 Existing surveys / 相邻综述 
- Priority: High background
- Training-free: N/A
- Original status: Verified by web
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

A Survey on Long-Video Storytelling Generation 属于 S0 Existing surveys / 相邻综述；当前根据题名、分类表与可用网页元信息定位为：与 narrative/entity memory 最接近，但它不是 memory lifecycle 综述。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 story / character / scene consistency 在 long storytelling architectures 载体上的建模、压缩、检索或评测。

## 4. Memory object

- story / character / scene consistency

## 5. Memory substrate

- long storytelling architectures

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 story / character / scene consistency 相关的历史状态或评测对象。 |
| Store | 主要载体：long storytelling architectures。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 与 narrative/entity memory 最接近，但它不是 memory lifecycle 综述。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S0 Existing surveys / 相邻综述 
- Role: 与 narrative/entity memory 最接近，但它不是 memory lifecycle 综述。
- Priority: High background


---

# ASurvey: Spatiotemporal Consistency in Video Generation

## 1. Metadata

- Year: 2025/2026
- ID: arXiv:2502.17863
- URL: https://arxiv.org/abs/2502.17863
- Category: S0 Existing surveys / 相邻综述 
- Priority: High background
- Training-free: N/A
- Original status: Verified by web
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

ASurvey: Spatiotemporal Consistency in Video Generation 属于 S0 Existing surveys / 相邻综述；当前根据题名、分类表与可用网页元信息定位为：说明 consistency survey 与 memory-centric survey 的差异：consistency 是现象层，memory 是机制层。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 spatiotemporal consistency 在 representation / generation / post-processing / metrics 载体上的建模、压缩、检索或评测。

## 4. Memory object

- spatiotemporal consistency

## 5. Memory substrate

- representation / generation / post-processing / metrics

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 spatiotemporal consistency 相关的历史状态或评测对象。 |
| Store | 主要载体：representation / generation / post-processing / metrics。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 说明 consistency survey 与 memory-centric survey 的差异：consistency 是现象层，memory 是机制层。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S0 Existing surveys / 相邻综述 
- Role: 说明 consistency survey 与 memory-centric survey 的差异：consistency 是现象层，memory 是机制层。
- Priority: High background


---

# Controllable Video Generation: A Survey

## 1. Metadata

- Year: 2025
- ID: arXiv:2507.16869
- URL: https://arxiv.org/abs/2507.16869
- Category: S0 Existing surveys / 相邻综述 
- Priority: Background
- Training-free: N/A
- Original status: From prior docs / To verify
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

Controllable Video Generation: A Survey 属于 S0 Existing surveys / 相邻综述；当前根据题名、分类表与可用网页元信息定位为：用于 reference/image/pose/depth/camera control 背景。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 control signals and reference consistency 在 control taxonomy 载体上的建模、压缩、检索或评测。

## 4. Memory object

- control signals and reference consistency

## 5. Memory substrate

- control taxonomy

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 control signals and reference consistency 相关的历史状态或评测对象。 |
| Store | 主要载体：control taxonomy。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 用于 reference/image/pose/depth/camera control 背景。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S0 Existing surveys / 相邻综述 
- Role: 用于 reference/image/pose/depth/camera control 背景。
- Priority: Background


---

# Survey of Video Diffusion Models

## 1. Metadata

- Year: 2025
- ID: arXiv:2504.16081
- URL: https://arxiv.org/abs/2504.16081
- Category: S0 Existing surveys / 相邻综述 
- Priority: Background
- Training-free: N/A
- Original status: Verified by web
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

Survey of Video Diffusion Models 属于 S0 Existing surveys / 相邻综述；当前根据题名、分类表与可用网页元信息定位为：作为最新 video diffusion survey 背景。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 video diffusion evolution and applications 在 architecture / optimization / applications 载体上的建模、压缩、检索或评测。

## 4. Memory object

- video diffusion evolution and applications

## 5. Memory substrate

- architecture / optimization / applications

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 video diffusion evolution and applications 相关的历史状态或评测对象。 |
| Store | 主要载体：architecture / optimization / applications。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 作为最新 video diffusion survey 背景。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S0 Existing surveys / 相邻综述 
- Role: 作为最新 video diffusion survey 背景。
- Priority: Background


---

# Video Diffusion Models: A Survey

## 1. Metadata

- Year: 2024
- ID: arXiv:2405.03150
- URL: https://arxiv.org/abs/2405.03150
- Category: S0 Existing surveys / 相邻综述 
- Priority: Background
- Training-free: N/A
- Original status: Verified by web
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

Video Diffusion Models: A Survey 属于 S0 Existing surveys / 相邻综述；当前根据题名、分类表与可用网页元信息定位为：作为视频扩散模型基础综述引用。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 video diffusion foundations 在 diffusion architecture taxonomy 载体上的建模、压缩、检索或评测。

## 4. Memory object

- video diffusion foundations

## 5. Memory substrate

- diffusion architecture taxonomy

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 video diffusion foundations 相关的历史状态或评测对象。 |
| Store | 主要载体：diffusion architecture taxonomy。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 作为视频扩散模型基础综述引用。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S0 Existing surveys / 相邻综述 
- Role: 作为视频扩散模型基础综述引用。
- Priority: Background


---

# Video Generation Models as World Models: Efficient Paradigms, Architectures and Algorithms

## 1. Metadata

- Year: 2026
- ID: arXiv:2603.28489
- URL: https://arxiv.org/abs/2603.28489
- Category: S0 Existing surveys / 相邻综述 
- Priority: High background
- Training-free: N/A
- Original status: Verified by web
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

Video Generation Models as World Models: Efficient Paradigms, Architectures and Algorithms 属于 S0 Existing surveys / 相邻综述；当前根据题名、分类表与可用网页元信息定位为：说明 world-model survey 主要从效率切入，不以 memory object/substrate 为主。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 efficient world modeling 在 efficient paradigms / architectures / inference 载体上的建模、压缩、检索或评测。

## 4. Memory object

- efficient world modeling

## 5. Memory substrate

- efficient paradigms / architectures / inference

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 efficient world modeling 相关的历史状态或评测对象。 |
| Store | 主要载体：efficient paradigms / architectures / inference。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 说明 world-model survey 主要从效率切入，不以 memory object/substrate 为主。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S0 Existing surveys / 相邻综述 
- Role: 说明 world-model survey 主要从效率切入，不以 memory object/substrate 为主。
- Priority: High background


---

# Video Generation Models in Robotics

## 1. Metadata

- Year: 2026
- ID: arXiv:2601.07823
- URL: https://arxiv.org/abs/2601.07823
- Category: S0 Existing surveys / 相邻综述 
- Priority: Background
- Training-free: N/A
- Original status: Verified by web
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

Video Generation Models in Robotics 属于 S0 Existing surveys / 相邻综述；当前根据题名、分类表与可用网页元信息定位为：用于 embodied/robotics world model 应用背景。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 embodied world models in robotics 在 video models for policy / planning / simulation 载体上的建模、压缩、检索或评测。

## 4. Memory object

- embodied world models in robotics

## 5. Memory substrate

- video models for policy / planning / simulation

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 embodied world models in robotics 相关的历史状态或评测对象。 |
| Store | 主要载体：video models for policy / planning / simulation。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 用于 embodied/robotics world model 应用背景。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S0 Existing surveys / 相邻综述 
- Role: 用于 embodied/robotics world model 应用背景。
- Priority: Background


---

# World Model for Robot Learning: A Comprehensive Survey

## 1. Metadata

- Year: 2026
- ID: arXiv:2605.00080
- URL: https://arxiv.org/html/2605.00080v1
- Category: S0 Existing surveys / 相邻综述 
- Priority: Background
- Training-free: N/A
- Original status: Verified by web
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

World Model for Robot Learning: A Comprehensive Survey 属于 S0 Existing surveys / 相邻综述；当前根据题名、分类表与可用网页元信息定位为：可在 world model background 中一笔带过。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 robot world model 在 policy-coupled world model taxonomy 载体上的建模、压缩、检索或评测。

## 4. Memory object

- robot world model

## 5. Memory substrate

- policy-coupled world model taxonomy

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 robot world model 相关的历史状态或评测对象。 |
| Store | 主要载体：policy-coupled world model taxonomy。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 可在 world model background 中一笔带过。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S0 Existing surveys / 相邻综述 
- Role: 可在 world model background 中一笔带过。
- Priority: Background


---

# Causal-Forcing

## 1. Metadata

- Year: 2026
- ID: arXiv:2602.02214
- URL: https://arxiv.org/abs/2602.02214
- Category: S1 AR / streaming video generation backbones 
- Priority: Core backbone
- Training-free: Trained
- Original status: From prior docs / To verify
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

Causal-Forcing 属于 S1 AR / streaming video generation backbones；当前根据题名、分类表与可用网页元信息定位为：与 Self/Rolling Forcing 并列的 AR backbone。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 block-causal AR video 在 framewise/chunkwise causal generation 载体上的建模、压缩、检索或评测。

## 4. Memory object

- block-causal AR video

## 5. Memory substrate

- framewise/chunkwise causal generation

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 block-causal AR video 相关的历史状态或评测对象。 |
| Store | 主要载体：framewise/chunkwise causal generation。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 与 Self/Rolling Forcing 并列的 AR backbone。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S1 AR / streaming video generation backbones 
- Role: 与 Self/Rolling Forcing 并列的 AR backbone。
- Priority: Core backbone


---

# CausVid

## 1. Metadata

- Year: 2024/2025
- ID: arXiv:2412.07772
- URL: https://arxiv.org/abs/2412.07772
- Category: S1 AR / streaming video generation backbones 
- Priority: Core background
- Training-free: Trained
- Original status: From prior docs / To verify
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

CausVid 属于 S1 AR / streaming video generation backbones；当前根据题名、分类表与可用网页元信息定位为：因果视频蒸馏和 KV cache baseline。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 causal long video rollout 在 block-causal + KV cache 载体上的建模、压缩、检索或评测。

## 4. Memory object

- causal long video rollout

## 5. Memory substrate

- block-causal + KV cache

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 causal long video rollout 相关的历史状态或评测对象。 |
| Store | 主要载体：block-causal + KV cache。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 因果视频蒸馏和 KV cache baseline。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S1 AR / streaming video generation backbones 
- Role: 因果视频蒸馏和 KV cache baseline。
- Priority: Core background


---

# Diffusion Forcing

## 1. Metadata

- Year: 2024
- ID: arXiv:2407.01392
- URL: https://arxiv.org/abs/2407.01392
- Category: S1 AR / streaming video generation backbones 
- Priority: Core background
- Training-free: Trained
- Original status: From prior docs / To verify
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

Diffusion Forcing 属于 S1 AR / streaming video generation backbones；当前根据题名、分类表与可用网页元信息定位为：Forcing 家族源头。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 long sequential generation 在 per-token independent noise / forcing family 载体上的建模、压缩、检索或评测。

## 4. Memory object

- long sequential generation

## 5. Memory substrate

- per-token independent noise / forcing family

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 long sequential generation 相关的历史状态或评测对象。 |
| Store | 主要载体：per-token independent noise / forcing family。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 Forcing 家族源头。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S1 AR / streaming video generation backbones 
- Role: Forcing 家族源头。
- Priority: Core background


---

# FramePack

## 1. Metadata

- Year: 2025
- ID: arXiv:2504.12626
- URL: https://arxiv.org/abs/2504.12626
- Category: S1 AR / streaming video generation backbones 
- Priority: Core background
- Training-free: Inference-time/system
- Original status: Verified by web
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

FramePack 属于 S1 AR / streaming video generation backbones；当前根据题名、分类表与可用网页元信息定位为：说明 context compression 是 memory substrate 之一。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 fixed-length historical context 在 frame/context packing 载体上的建模、压缩、检索或评测。

## 4. Memory object

- fixed-length historical context

## 5. Memory substrate

- frame/context packing

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 fixed-length historical context 相关的历史状态或评测对象。 |
| Store | 主要载体：frame/context packing。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 说明 context compression 是 memory substrate 之一。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S1 AR / streaming video generation backbones 
- Role: 说明 context compression 是 memory substrate 之一。
- Priority: Core background


---

# Infinity-RoPE

## 1. Metadata

- Year: 2025/2026
- ID: arXiv:2511.20649
- URL: https://arxiv.org/abs/2511.20649
- Category: S1 AR / streaming video generation backbones 
- Priority: Core related
- Training-free: Inference/system
- Original status: Verified by web
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

Infinity-RoPE 属于 S1 AR / streaming video generation backbones；当前根据题名、分类表与可用网页元信息定位为：同时属于 positional memory；适合作为 long action rollout 的位置策略。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 infinite action-controllable rollout 在 Block-Relativistic RoPE / KV Flush / RoPE Cut 载体上的建模、压缩、检索或评测。

## 4. Memory object

- infinite action-controllable rollout

## 5. Memory substrate

- Block-Relativistic RoPE / KV Flush / RoPE Cut

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 infinite action-controllable rollout 相关的历史状态或评测对象。 |
| Store | 主要载体：Block-Relativistic RoPE / KV Flush / RoPE Cut。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 同时属于 positional memory；适合作为 long action rollout 的位置策略。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S1 AR / streaming video generation backbones 
- Role: 同时属于 positional memory；适合作为 long action rollout 的位置策略。
- Priority: Core related


---

# LongLive

## 1. Metadata

- Year: 2025
- ID: arXiv:2509.22622
- URL: https://arxiv.org/abs/2509.22622
- Category: S1 AR / streaming video generation backbones 
- Priority: Core backbone
- Training-free: LoRA-trained / system
- Original status: Verified by web
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

LongLive 属于 S1 AR / streaming video generation backbones；当前根据题名、分类表与可用网页元信息定位为：长视频 AR memory 重要平台；适合做 taxonomy 背景。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 long-horizon frame-level AR memory 在 causal attention / KV recache / frame sink 载体上的建模、压缩、检索或评测。

## 4. Memory object

- long-horizon frame-level AR memory

## 5. Memory substrate

- causal attention / KV recache / frame sink

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 long-horizon frame-level AR memory 相关的历史状态或评测对象。 |
| Store | 主要载体：causal attention / KV recache / frame sink。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 长视频 AR memory 重要平台；适合做 taxonomy 背景。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S1 AR / streaming video generation backbones 
- Role: 长视频 AR memory 重要平台；适合做 taxonomy 背景。
- Priority: Core backbone


---

# LongLive-2.0

## 1. Metadata

- Year: 2026
- ID: arXiv:2605.18739
- URL: https://arxiv.org/abs/2605.18739
- Category: S1 AR / streaming video generation backbones 
- Priority: Related
- Training-free: Trained/system
- Original status: Verified by web
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

LongLive-2.0 属于 S1 AR / streaming video generation backbones；当前根据题名、分类表与可用网页元信息定位为：用于效率和系统侧背景。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 efficient long video generation 在 NVFP4/system optimization + long video inference 载体上的建模、压缩、检索或评测。

## 4. Memory object

- efficient long video generation

## 5. Memory substrate

- NVFP4/system optimization + long video inference

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 efficient long video generation 相关的历史状态或评测对象。 |
| Store | 主要载体：NVFP4/system optimization + long video inference。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 用于效率和系统侧背景。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S1 AR / streaming video generation backbones 
- Role: 用于效率和系统侧背景。
- Priority: Related


---

# MAGI-1

## 1. Metadata

- Year: 2025
- ID: arXiv?
- URL: N/A
- Category: S1 AR / streaming video generation backbones 
- Priority: Related
- Training-free: Trained
- Original status: From prior docs / To verify
- Reading status: metadata-only
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

MAGI-1 属于 S1 AR / streaming video generation backbones；当前根据题名、分类表与可用网页元信息定位为：长视频 AR 代表，可背景引用。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 autoregressive video generation 在 chunk/segment AR 载体上的建模、压缩、检索或评测。

## 4. Memory object

- autoregressive video generation

## 5. Memory substrate

- chunk/segment AR

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 autoregressive video generation 相关的历史状态或评测对象。 |
| Store | 主要载体：chunk/segment AR。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 长视频 AR 代表，可背景引用。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S1 AR / streaming video generation backbones 
- Role: 长视频 AR 代表，可背景引用。
- Priority: Related


---

# Pyramid Flow

## 1. Metadata

- Year: 2024/2025
- ID: arXiv?
- URL: N/A
- Category: S1 AR / streaming video generation backbones 
- Priority: Background
- Training-free: Trained
- Original status: From prior docs / To verify
- Reading status: metadata-only
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

Pyramid Flow 属于 S1 AR / streaming video generation backbones；当前根据题名、分类表与可用网页元信息定位为：视频生成基础模型背景。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 pyramid flow video generation 在 multi-scale flow matching 载体上的建模、压缩、检索或评测。

## 4. Memory object

- pyramid flow video generation

## 5. Memory substrate

- multi-scale flow matching

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 pyramid flow video generation 相关的历史状态或评测对象。 |
| Store | 主要载体：multi-scale flow matching。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 视频生成基础模型背景。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S1 AR / streaming video generation backbones 
- Role: 视频生成基础模型背景。
- Priority: Background


---

# Rolling Forcing

## 1. Metadata

- Year: 2025
- ID: arXiv:2509.25161
- URL: https://arxiv.org/abs/2509.25161
- Category: S1 AR / streaming video generation backbones 
- Priority: Core backbone
- Training-free: Trained
- Original status: Verified by web
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

Rolling Forcing 属于 S1 AR / streaming video generation backbones；当前根据题名、分类表与可用网页元信息定位为：主流 streaming AR backbone；attention sink 是全局锚点记忆。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 rolling historical context 在 rolling-window joint denoising + frame-0 attention sink 载体上的建模、压缩、检索或评测。

## 4. Memory object

- rolling historical context

## 5. Memory substrate

- rolling-window joint denoising + frame-0 attention sink

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 rolling historical context 相关的历史状态或评测对象。 |
| Store | 主要载体：rolling-window joint denoising + frame-0 attention sink。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 主流 streaming AR backbone；attention sink 是全局锚点记忆。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S1 AR / streaming video generation backbones 
- Role: 主流 streaming AR backbone；attention sink 是全局锚点记忆。
- Priority: Core backbone


---

# SANA-Video

## 1. Metadata

- Year: 2025
- ID: arXiv:2509.24695
- URL: https://arxiv.org/abs/2509.24695
- Category: S1 AR / streaming video generation backbones 
- Priority: Related
- Training-free: Trained/system
- Original status: From prior docs / To verify
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

SANA-Video 属于 S1 AR / streaming video generation backbones；当前根据题名、分类表与可用网页元信息定位为：用于 efficiency/memory budget 讨论。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 efficient long video generation 在 linear attention / constant-memory KV cache 载体上的建模、压缩、检索或评测。

## 4. Memory object

- efficient long video generation

## 5. Memory substrate

- linear attention / constant-memory KV cache

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 efficient long video generation 相关的历史状态或评测对象。 |
| Store | 主要载体：linear attention / constant-memory KV cache。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 用于 efficiency/memory budget 讨论。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S1 AR / streaming video generation backbones 
- Role: 用于 efficiency/memory budget 讨论。
- Priority: Related


---

# Self Forcing

## 1. Metadata

- Year: 2025
- ID: arXiv:2506.08009
- URL: https://arxiv.org/abs/2506.08009
- Category: S1 AR / streaming video generation backbones 
- Priority: Core backbone
- Training-free: Trained
- Original status: Verified by web
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

Self Forcing 属于 S1 AR / streaming video generation backbones；当前根据题名、分类表与可用网页元信息定位为：后续 training-free memory 方法的重要 backbone。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 self-generated rollout memory 在 KV-cache enabled autoregressive training 载体上的建模、压缩、检索或评测。

## 4. Memory object

- self-generated rollout memory

## 5. Memory substrate

- KV-cache enabled autoregressive training

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 self-generated rollout memory 相关的历史状态或评测对象。 |
| Store | 主要载体：KV-cache enabled autoregressive training。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 后续 training-free memory 方法的重要 backbone。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S1 AR / streaming video generation backbones 
- Role: 后续 training-free memory 方法的重要 backbone。
- Priority: Core backbone


---

# SkyReels-V2

## 1. Metadata

- Year: 2025
- ID: arXiv?
- URL: N/A
- Category: S1 AR / streaming video generation backbones 
- Priority: Related
- Training-free: Trained
- Original status: From prior docs / To verify
- Reading status: metadata-only
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

SkyReels-V2 属于 S1 AR / streaming video generation backbones；当前根据题名、分类表与可用网页元信息定位为：开放视频生成 backbone 背景。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 long video / streaming generation 在 sliding-window causal generation 载体上的建模、压缩、检索或评测。

## 4. Memory object

- long video / streaming generation

## 5. Memory substrate

- sliding-window causal generation

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 long video / streaming generation 相关的历史状态或评测对象。 |
| Store | 主要载体：sliding-window causal generation。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 开放视频生成 backbone 背景。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S1 AR / streaming video generation backbones 
- Role: 开放视频生成 backbone 背景。
- Priority: Related


---

# StreamDiT

## 1. Metadata

- Year: 2025
- ID: arXiv:2507.03745
- URL: https://arxiv.org/abs/2507.03745
- Category: S1 AR / streaming video generation backbones 
- Priority: Related
- Training-free: Trained/system
- Original status: Mentioned by web search; To verify details
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

StreamDiT 属于 S1 AR / streaming video generation backbones；当前根据题名、分类表与可用网页元信息定位为：LongLive-RAG references 中的 AR rollout 背景。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 real-time streaming generation 在 DiT streaming architecture 载体上的建模、压缩、检索或评测。

## 4. Memory object

- real-time streaming generation

## 5. Memory substrate

- DiT streaming architecture

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 real-time streaming generation 相关的历史状态或评测对象。 |
| Store | 主要载体：DiT streaming architecture。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 LongLive-RAG references 中的 AR rollout 背景。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S1 AR / streaming video generation backbones 
- Role: LongLive-RAG references 中的 AR rollout 背景。
- Priority: Related


---

# StreamingT2V

## 1. Metadata

- Year: 2025
- ID: arXiv?
- URL: N/A
- Category: S1 AR / streaming video generation backbones 
- Priority: Related
- Training-free: Trained / system
- Original status: From prior docs / To verify
- Reading status: metadata-only
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

StreamingT2V 属于 S1 AR / streaming video generation backbones；当前根据题名、分类表与可用网页元信息定位为：作为 streaming video generation 背景。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 streaming text-to-video memory 在 short/long memory for streaming 载体上的建模、压缩、检索或评测。

## 4. Memory object

- streaming text-to-video memory

## 5. Memory substrate

- short/long memory for streaming

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 streaming text-to-video memory 相关的历史状态或评测对象。 |
| Store | 主要载体：short/long memory for streaming。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 作为 streaming video generation 背景。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S1 AR / streaming video generation backbones 
- Role: 作为 streaming video generation 背景。
- Priority: Related


---

# Video generation models as world simulators / Sora technical report page

## 1. Metadata

- Year: 2024
- ID: OpenAI blog
- URL: https://openai.com/index/video-generation-models-as-world-simulators/
- Category: S1 AR / streaming video generation backbones 
- Priority: Background
- Training-free: N/A
- Original status: Verified by web
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

Video generation models as world simulators / Sora technical report page 属于 S1 AR / streaming video generation backbones；当前根据题名、分类表与可用网页元信息定位为：用于引出“video generation -> world simulators”的叙事。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 emergent physical/world simulation 在 large-scale video generation model 载体上的建模、压缩、检索或评测。

## 4. Memory object

- emergent physical/world simulation

## 5. Memory substrate

- large-scale video generation model

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 emergent physical/world simulation 相关的历史状态或评测对象。 |
| Store | 主要载体：large-scale video generation model。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 用于引出“video generation -> world simulators”的叙事。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S1 AR / streaming video generation backbones 
- Role: 用于引出“video generation -> world simulators”的叙事。
- Priority: Background


---

# Wan: Open and Advanced Large-Scale Video Generative Models

## 1. Metadata

- Year: 2025
- ID: arXiv:2503.20314
- URL: https://arxiv.org/abs/2503.20314
- Category: S1 AR / streaming video generation backbones 
- Priority: Core background
- Training-free: Trained
- Original status: Mentioned by web; To verify details
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

Wan: Open and Advanced Large-Scale Video Generative Models 属于 S1 AR / streaming video generation backbones；当前根据题名、分类表与可用网页元信息定位为：很多 2026 memory 方法基于 Wan2.1/2.2，需要作为 backbone 背景引用。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 base T2V/I2V backbone 在 large-scale video generative model 载体上的建模、压缩、检索或评测。

## 4. Memory object

- base T2V/I2V backbone

## 5. Memory substrate

- large-scale video generative model

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 base T2V/I2V backbone 相关的历史状态或评测对象。 |
| Store | 主要载体：large-scale video generative model。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 很多 2026 memory 方法基于 Wan2.1/2.2，需要作为 backbone 背景引用。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S1 AR / streaming video generation backbones 
- Role: 很多 2026 memory 方法基于 Wan2.1/2.2，需要作为 backbone 背景引用。
- Priority: Core background


---

# Attention Sink / StreamingLLM

## 1. Metadata

- Year: 2023/2024
- ID: arXiv:2309.17453
- URL: https://arxiv.org/abs/2309.17453
- Category: S2 KV cache / attention memory 
- Priority: Background
- Training-free: Yes
- Original status: From prior docs / To verify
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

Attention Sink / StreamingLLM 属于 S2 KV cache / attention memory；当前根据题名、分类表与可用网页元信息定位为：视频 attention sink 的通用基础；不属于视频论文但必须解释来源。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 persistent sink tokens 在 initial sink + sliding window 载体上的建模、压缩、检索或评测。

## 4. Memory object

- persistent sink tokens

## 5. Memory substrate

- initial sink + sliding window

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 persistent sink tokens 相关的历史状态或评测对象。 |
| Store | 主要载体：initial sink + sliding window。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 视频 attention sink 的通用基础；不属于视频论文但必须解释来源。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S2 KV cache / attention memory 
- Role: 视频 attention sink 的通用基础；不属于视频论文但必须解释来源。
- Priority: Background


---

# DecMem: Decoupled Global and Local Memory for Minute-long Video Generation

## 1. Metadata

- Year: 2026
- ID: arXiv:2605.31336
- URL: https://arxiv.org/abs/2605.31336
- Category: S2 KV cache / attention memory 
- Priority: High
- Training-free: Likely yes / system
- Original status: From prior docs / To verify
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

DecMem: Decoupled Global and Local Memory for Minute-long Video Generation 属于 S2 KV cache / attention memory；当前根据题名、分类表与可用网页元信息定位为：全局/局部 memory 解耦代表。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 global-local memory 在 Sparse Global Memory + Anchored Local Memory 载体上的建模、压缩、检索或评测。

## 4. Memory object

- global-local memory

## 5. Memory substrate

- Sparse Global Memory + Anchored Local Memory

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 global-local memory 相关的历史状态或评测对象。 |
| Store | 主要载体：Sparse Global Memory + Anchored Local Memory。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 全局/局部 memory 解耦代表。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S2 KV cache / attention memory 
- Role: 全局/局部 memory 解耦代表。
- Priority: High


---

# Deep Forcing: Training-Free Long Video Generation with Deep Sink and Participative Compression

## 1. Metadata

- Year: 2025
- ID: arXiv:2512.05081
- URL: https://arxiv.org/abs/2512.05081
- Category: S2 KV cache / attention memory 
- Priority: Must-read
- Training-free: Yes
- Original status: Verified by web
- Reading status: web-read
- Source basis: arXiv abstract web-read

## 2. One-sentence takeaway

Deep Forcing shows that naively importing attention sinks into video diffusion can harm fidelity and motion; its deep sink and participative compression preserve useful history without over-freezing the rollout.

## 3. Problem / failure mode

AR video diffusion still suffers temporal repetition, drift and motion deceleration; naive StreamingLLM-style sinks cause fidelity degradation and motion stagnation.

## 4. Memory object

- global context + active recent tokens

## 5. Memory substrate

- deep sink + participative compression

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | Writes persistent sink tokens from history and records recently active attention participants. |
| Store | Half sliding window as persistent sink tokens plus compressed active KV history. |
| Retrieve | Attention reuses deep sink context and recently active compressed tokens. |
| Use | Stabilizes global context while maintaining dynamic degree for 12x horizon extrapolation. |
| Update | Participative compression updates retained history according to recent attention participation. |
| Forget | Prunes redundant/degraded tokens and discards inactive history. |
| Evaluate | Reports 5s-trained to 60s+ generation, real-time setting, image/aesthetic quality, consistency and dynamic degree. |

## 7. Strengths for this survey

Important corrective to “more sink is always better.” Useful in evaluation discussion about frozen-video traps.

## 8. Limitations / second-pass PDF checks

Still implicit token memory; no explicit entity/scene semantics.

## 9. Recommended placement

- Main category: S2 KV cache / attention memory 
- Role: 说明 attention sink 不是越强越好；过强会 motion stagnation。
- Priority: Must-read


---

# Echo-Forcing: A Scene Memory Framework for Interactive Long Video Generation

## 1. Metadata

- Year: 2026
- ID: arXiv:2605.16003
- URL: https://arxiv.org/abs/2605.16003
- Category: S2 KV cache / attention memory 
- Priority: Must-read
- Training-free: Yes
- Original status: Verified by web
- Reading status: web-read
- Source basis: arXiv abstract web-read

## 2. One-sentence takeaway

Echo-Forcing is the clearest scene-memory paper: it reframes long-video degradation as functional entanglement of historical KV states and explicitly separates stable anchors, compressed history, recent windows, recall, and conflict-aware forgetting.

## 3. Problem / failure mode

Interactive AR video generation under prompt switching suffers from old-scene forgetting, delayed prompt response, and background contamination because all historical KV tokens are handled by one cache policy.

## 4. Memory object

- scene memory / historical scene recall

## 5. Memory substrate

- hierarchical temporal KV memory

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | Writes historical scene evidence into hierarchical temporal KV states; old scenes can be crystallized as Scene Recall Frames. |
| Store | Stable anchors, compressed history, and recent windows under relative RoPE; scene recall frames store spatially structured KV representations. |
| Retrieve | Retrieves long-range scene evidence through Scene Recall Frames when old scenes need to reappear. |
| Use | Injects/reuses recalled historical KV evidence during generation to support smooth transitions, hard cuts, and old-scene recall. |
| Update | Updates bounded cache by maintaining recent dynamics and compressed history instead of keeping a uniform sliding window. |
| Forget | Difference-aware Memory Decay adaptively suppresses tokens that conflict with the current/new scene. |
| Evaluate | VBench-Long and interactive long-video settings; should be paired with dynamic/anti-loop metrics because stable recall may otherwise be over-rewarded. |

## 7. Strengths for this survey

Best representative for the preserve-recall-forget lifecycle; ideal as the anchor example in Section 4.

## 8. Limitations / second-pass PDF checks

Scene-centric rather than entity-centric; it remembers what/where a scene is more than who an entity is.

## 9. Recommended placement

- Main category: S2 KV cache / attention memory 
- Role: 场景记忆核心论文：stable anchor、compressed history、recent window、scene recall、difference-aware decay。
- Priority: Must-read


---

# Future Forcing: Future-aware Training-free KV Cache Policy for Autoregressive Video Generation

## 1. Metadata

- Year: 2026
- ID: arXiv:2605.30083
- URL: https://arxiv.org/abs/2605.30083
- Category: S2 KV cache / attention memory 
- Priority: High
- Training-free: Yes
- Original status: Verified by web
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

Future Forcing: Future-aware Training-free KV Cache Policy for Autoregressive Video Generation 属于 S2 KV cache / attention memory；当前根据题名、分类表与可用网页元信息定位为：从 past-aware cache 走向 future-aware cache。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 future-useful historical tokens 在 future query proxy + cache policy 载体上的建模、压缩、检索或评测。

## 4. Memory object

- future-useful historical tokens

## 5. Memory substrate

- future query proxy + cache policy

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 future-useful historical tokens 相关的历史状态或评测对象。 |
| Store | 主要载体：future query proxy + cache policy。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 从 past-aware cache 走向 future-aware cache。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S2 KV cache / attention memory 
- Role: 从 past-aware cache 走向 future-aware cache。
- Priority: High


---

# H2O / SnapKV / Quest / PyramidKV / SirLLM

## 1. Metadata

- Year: 2023-2025
- ID: LLM KV cache literature
- URL: N/A
- Category: S2 KV cache / attention memory 
- Priority: Background
- Training-free: Yes
- Original status: From prior docs / To verify individually
- Reading status: metadata-only
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

H2O / SnapKV / Quest / PyramidKV / SirLLM 属于 S2 KV cache / attention memory；当前根据题名、分类表与可用网页元信息定位为：作为 KV cache policy 的通用来源，不要喧宾夺主。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 generic KV cache selection/compression 在 heavy hitter / query-aware / layer budget / entropy retention 载体上的建模、压缩、检索或评测。

## 4. Memory object

- generic KV cache selection/compression

## 5. Memory substrate

- heavy hitter / query-aware / layer budget / entropy retention

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 generic KV cache selection/compression 相关的历史状态或评测对象。 |
| Store | 主要载体：heavy hitter / query-aware / layer budget / entropy retention。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 作为 KV cache policy 的通用来源，不要喧宾夺主。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S2 KV cache / attention memory 
- Role: 作为 KV cache policy 的通用来源，不要喧宾夺主。
- Priority: Background


---

# KV Cache Quantization for Self-Forcing Video Generation: A 33-Method Empirical Study

## 1. Metadata

- Year: 2026
- ID: arXiv:2603.27469
- URL: https://arxiv.org/abs/2603.27469
- Category: S2 KV cache / attention memory 
- Priority: High
- Training-free: Empirical/system
- Original status: Verified by web, added in v0.2
- Reading status: web-read
- Source basis: arXiv abstract web-read

## 2. One-sentence takeaway

This paper is a systems-memory study: it shows that nominal KV compression is not enough; realized VRAM, runtime, buffer reconstruction and terminal drift matter.

## 3. Problem / failure mode

Self-forcing long videos expose KV-cache growth as a systems bottleneck.

## 4. Memory object

- system memory / KV budget

## 5. Memory substrate

- KV-cache quantization and compression policies

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | Not about semantic write; evaluates cache policies and quantization variants in self-forcing stack. |
| Store | Quantized/pruned KV caches including FlowCache-inspired soft-prune INT4 and other methods. |
| Retrieve | Retrieval follows attention cache access; emphasis is on storage/compression behavior. |
| Use | Compresses cache to reduce VRAM while preserving generation fidelity. |
| Update | Evaluates refresh stages and integration costs. |
| Forget | Soft-prune and cache policy variants implement system-level eviction/compression. |
| Evaluate | 33 variants, 610 prompt-level observations, 63 benchmark summaries, MovieGen/StoryEval, VRAM/runtime/SSIM/LPIPS/PSNR/terminal drift. |

## 7. Strengths for this survey

Essential for system memory section: compression ratio != deployment benefit.

## 8. Limitations / second-pass PDF checks

Not a semantic memory method; complements rather than replaces identity/scene memory.

## 9. Recommended placement

- Main category: S2 KV cache / attention memory 
- Role: 补充系统维度：量化/压缩不只是省显存，也会影响 drift、fidelity 与实际部署。
- Priority: High


---

# LongLive-RAG: A General Retrieval-Augmented Framework for Long Video Generation

## 1. Metadata

- Year: 2026
- ID: arXiv:2606.02553
- URL: https://arxiv.org/abs/2606.02553
- Category: S2 KV cache / attention memory 
- Priority: High
- Training-free: System / may be training-light
- Original status: Verified by web
- Reading status: web-read
- Source basis: arXiv/search abstract web-read

## 2. One-sentence takeaway

LongLive-RAG is the cleanest RAG formulation for AR video generation: previously generated latents become a content-addressable searchable history, not just a sliding window.

## 3. Problem / failure mode

Sliding-window attention creates irreversible error accumulation because later blocks condition only on degraded recent trajectory.

## 4. Memory object

- retrieval over long video context

## 5. Memory substrate

- retrieval-augmented historical context

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | Writes self-generated latents into dynamic searchable history. |
| Store | Content-addressable latent memory indexed by query embeddings. |
| Retrieve | At each new block, retrieves relevant historical latents; Window Temporal Delta Loss makes embeddings capture meaningful temporal changes. |
| Use | Conditions generator on non-local historical context to reduce drift. |
| Update | History grows with generated latents; retrieval index updates dynamically. |
| Forget | Not central; retrieval rather than permanent retention handles relevance. |
| Evaluate | Multiple AR backbones/lengths and VBench-Long rank. |

## 7. Strengths for this survey

Core S6 retrieval-memory paper for video generation.

## 8. Limitations / second-pass PDF checks

Retrieval quality depends on embedding/indexing; not explicitly entity-aware.

## 9. Recommended placement

- Main category: S2 KV cache / attention memory 
- Role: 把 RAG 思路引入 LongLive/long video memory。
- Priority: High


---

# LVSA: Training-Free Sparse Attention for Long Video Diffusion

## 1. Metadata

- Year: 2026
- ID: arXiv:2605.31057
- URL: https://arxiv.org/abs/2605.31057
- Category: S2 KV cache / attention memory 
- Priority: High
- Training-free: Yes
- Original status: Verified by web
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

LVSA: Training-Free Sparse Attention for Long Video Diffusion 属于 S2 KV cache / attention memory；当前根据题名、分类表与可用网页元信息定位为：旋转全局锚点与 VQeval；提醒普通 consistency metric 可能奖励 loop/frozen video。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 global anchor / anti-loop memory 在 block-sparse attention + rotating global anchors 载体上的建模、压缩、检索或评测。

## 4. Memory object

- global anchor / anti-loop memory

## 5. Memory substrate

- block-sparse attention + rotating global anchors

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 global anchor / anti-loop memory 相关的历史状态或评测对象。 |
| Store | 主要载体：block-sparse attention + rotating global anchors。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 旋转全局锚点与 VQeval；提醒普通 consistency metric 可能奖励 loop/frozen video。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S2 KV cache / attention memory 
- Role: 旋转全局锚点与 VQeval；提醒普通 consistency metric 可能奖励 loop/frozen video。
- Priority: High


---

# MAG: Memorize-and-Generate for Long Video Generation

## 1. Metadata

- Year: 2025
- ID: arXiv:2512.18741
- URL: https://arxiv.org/html/2512.18741v1
- Category: S2 KV cache / attention memory 
- Priority: High
- Training-free: Trained/system
- Original status: Verified by web
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

MAG: Memorize-and-Generate for Long Video Generation 属于 S2 KV cache / attention memory；当前根据题名、分类表与可用网页元信息定位为：将 memory compression 和 generation 解耦，适合 retrieval memory 章节。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 history scene consistency 在 memory compression model + generation model 载体上的建模、压缩、检索或评测。

## 4. Memory object

- history scene consistency

## 5. Memory substrate

- memory compression model + generation model

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 history scene consistency 相关的历史状态或评测对象。 |
| Store | 主要载体：memory compression model + generation model。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 将 memory compression 和 generation 解耦，适合 retrieval memory 章节。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S2 KV cache / attention memory 
- Role: 将 memory compression 和 generation 解耦，适合 retrieval memory 章节。
- Priority: High


---

# MemRoPE: Training-Free Infinite Video Generation via Evolving Memory Tokens

## 1. Metadata

- Year: 2026
- ID: arXiv:2603.12513
- URL: https://arxiv.org/abs/2603.12513
- Category: S2 KV cache / attention memory 
- Priority: Must-read
- Training-free: Yes
- Original status: Verified by web
- Reading status: web-read
- Source basis: arXiv abstract web-read

## 2. One-sentence takeaway

MemRoPE co-designs content compression and temporal coordinates: long/short EMA memory tokens only make sense if keys are cached before RoPE and positional embeddings are applied online.

## 3. Problem / failure mode

Sliding-window caches discard past context, causing fidelity degradation, identity drift, and motion stagnation; static attention sinks cannot reflect evolving video content.

## 4. Memory object

- identity + recent dynamics compressed memory

## 5. Memory substrate

- dual EMA memory tokens + unrotated KV + online RoPE

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | Continuously writes all past keys into dual long-term and short-term EMA streams. |
| Store | Fixed-size memory tokens plus unrotated key cache. |
| Retrieve | Attention retrieves aggregated long/short memory with RoPE applied dynamically at attention time. |
| Use | Provides global identity/history and recent dynamics within fixed cache for unbounded generation. |
| Update | EMA updates long-term and short-term memory streams as video grows. |
| Forget | Forgetting is implicit through EMA decay rather than explicit semantic conflict detection. |
| Evaluate | Minute- to hour-scale generation with temporal coherence, visual fidelity and subject consistency. |

## 7. Strengths for this survey

Key paper for positional memory: content memory cannot be separated from coordinate design.

## 8. Limitations / second-pass PDF checks

EMA memory is still implicit and does not know which entity or attribute is being stored.

## 9. Recommended placement

- Main category: S2 KV cache / attention memory 
- Role: 把 memory compression 和 RoPE phase 解耦联系起来，技术主线非常关键。
- Priority: Must-read


---

# OmniMem: A General Retrieval-Augmented Framework for Long Video Generation / full-range KV retrieval

## 1. Metadata

- Year: 2026
- ID: arXiv:2605.30519
- URL: https://arxiv.org/abs/2605.30519
- Category: S2 KV cache / attention memory 
- Priority: High
- Training-free: Likely yes / system
- Original status: Verified by web title/abstract search
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

OmniMem: A General Retrieval-Augmented Framework for Long Video Generation / full-range KV retrieval 属于 S2 KV cache / attention memory；当前根据题名、分类表与可用网页元信息定位为：用于 full-range sparse memory retrieval 分类。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 long-range sparse KV memory 在 sparse scattered KV retrieval 载体上的建模、压缩、检索或评测。

## 4. Memory object

- long-range sparse KV memory

## 5. Memory substrate

- sparse scattered KV retrieval

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 long-range sparse KV memory 相关的历史状态或评测对象。 |
| Store | 主要载体：sparse scattered KV retrieval。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 用于 full-range sparse memory retrieval 分类。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S2 KV cache / attention memory 
- Role: 用于 full-range sparse memory retrieval 分类。
- Priority: High


---

# Pack and Force Your Memory / MemoryPack

## 1. Metadata

- Year: 2025
- ID: arXiv:2510.01784
- URL: https://arxiv.org/html/2510.01784v1
- Category: S2 KV cache / attention memory 
- Priority: Related
- Training-free: Trained/system
- Original status: Verified by web
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

Pack and Force Your Memory / MemoryPack 属于 S2 KV cache / attention memory；当前根据题名、分类表与可用网页元信息定位为：context packing 与 retrieval memory 背景。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 short/long dependencies 在 learnable context retrieval / packed memory 载体上的建模、压缩、检索或评测。

## 4. Memory object

- short/long dependencies

## 5. Memory substrate

- learnable context retrieval / packed memory

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 short/long dependencies 相关的历史状态或评测对象。 |
| Store | 主要载体：learnable context retrieval / packed memory。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 context packing 与 retrieval memory 背景。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S2 KV cache / attention memory 
- Role: context packing 与 retrieval memory 背景。
- Priority: Related


---

# Pyramid Forcing: Head-Aware Pyramid KV Cache Policy for High-Quality Long Video Generation

## 1. Metadata

- Year: 2026
- ID: arXiv:2605.13111
- URL: https://arxiv.org/abs/2605.13111
- Category: S2 KV cache / attention memory 
- Priority: Must-read
- Training-free: Yes
- Original status: Verified by web
- Reading status: web-read
- Source basis: arXiv abstract web-read

## 2. One-sentence takeaway

Pyramid Forcing makes attention-head heterogeneity central: Anchor, Wave, and Veil heads have different temporal dependencies and therefore need different KV policies.

## 3. Problem / failure mode

Unified historical-frame retention assumes homogeneous dependencies across attention heads and causes long-term degradation.

## 4. Memory object

- head-specific temporal memory

## 5. Memory substrate

- Anchor/Wave/Veil per-head KV cache

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | Profiles attention heads offline and writes history into heterogeneous head-aware cache structures. |
| Store | Ragged/pyramidal KV cache with behavior-specific cache lengths and policies. |
| Retrieve | Anchor heads access broad context; Wave heads retrieve periodic dependencies; Veil heads focus on initial/adjacent frames. |
| Use | Improves long-horizon generation quality on Self Forcing and Causal Forcing. |
| Update | Cache is updated according to head type rather than global FIFO policy. |
| Forget | Implicit through per-head cache policy and retention budgets. |
| Evaluate | VBench-Long; reported 60s Self Forcing score improvement from 77.87 to 81.21. |

## 7. Strengths for this survey

Core reference for head/layer-specialized memory and a natural precursor to identity/motion/layout head routing.

## 8. Limitations / second-pass PDF checks

Head types are temporal roles, not explicit semantic roles such as identity/entity/layout.

## 9. Recommended placement

- Main category: S2 KV cache / attention memory 
- Role: head-aware memory 的核心论文；可以引出 identity/motion/layout head specialization。
- Priority: Must-read


---

# Sparse VideoGen (SVG)

## 1. Metadata

- Year: 2025
- ID: ICLR 2025?
- URL: N/A
- Category: S2 KV cache / attention memory 
- Priority: Related
- Training-free: Yes
- Original status: From prior docs / To verify
- Reading status: metadata-only
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

Sparse VideoGen (SVG) 属于 S2 KV cache / attention memory；当前根据题名、分类表与可用网页元信息定位为：Pyramid Forcing / HRMR 类 head-aware 方法的前置背景。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 spatial/temporal attention head roles 在 online head classification / sparse video attention 载体上的建模、压缩、检索或评测。

## 4. Memory object

- spatial/temporal attention head roles

## 5. Memory substrate

- online head classification / sparse video attention

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 spatial/temporal attention head roles 相关的历史状态或评测对象。 |
| Store | 主要载体：online head classification / sparse video attention。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 Pyramid Forcing / HRMR 类 head-aware 方法的前置背景。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S2 KV cache / attention memory 
- Role: Pyramid Forcing / HRMR 类 head-aware 方法的前置背景。
- Priority: Related


---

# SWIFT: Semantic Injection Cache and Adaptive Dynamic Window

## 1. Metadata

- Year: 2026
- ID: arXiv:2605.09442
- URL: https://arxiv.org/html/2605.09442v1
- Category: S2 KV cache / attention memory 
- Priority: Related
- Training-free: Likely yes / inference
- Original status: From prior docs / To verify
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

SWIFT: Semantic Injection Cache and Adaptive Dynamic Window 属于 S2 KV cache / attention memory；当前根据题名、分类表与可用网页元信息定位为：适合 multi-prompt memory 分类。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 multi-prompt semantic continuity 在 semantic injection cache + dynamic window 载体上的建模、压缩、检索或评测。

## 4. Memory object

- multi-prompt semantic continuity

## 5. Memory substrate

- semantic injection cache + dynamic window

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 multi-prompt semantic continuity 相关的历史状态或评测对象。 |
| Store | 主要载体：semantic injection cache + dynamic window。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 适合 multi-prompt memory 分类。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S2 KV cache / attention memory 
- Role: 适合 multi-prompt memory 分类。
- Priority: Related


---

# FLEX / Train Short, Inference Long

## 1. Metadata

- Year: 2026
- ID: arXiv:2602.14027
- URL: https://arxiv.org/abs/2602.14027
- Category: S3 Positional / RoPE memory 
- Priority: High
- Training-free: Yes
- Original status: Verified by web
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

FLEX / Train Short, Inference Long 属于 S3 Positional / RoPE memory；当前根据题名、分类表与可用网页元信息定位为：把 3D RoPE spectral bias 和动态先验不足联系起来。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 temporal horizon extension 在 frequency-aware RoPE modulation + antiphase noise + sink 载体上的建模、压缩、检索或评测。

## 4. Memory object

- temporal horizon extension

## 5. Memory substrate

- frequency-aware RoPE modulation + antiphase noise + sink

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 temporal horizon extension 相关的历史状态或评测对象。 |
| Store | 主要载体：frequency-aware RoPE modulation + antiphase noise + sink。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 把 3D RoPE spectral bias 和动态先验不足联系起来。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S3 Positional / RoPE memory 
- Role: 把 3D RoPE spectral bias 和动态先验不足联系起来。
- Priority: High


---

# FreeLOC

## 1. Metadata

- Year: 2026
- ID: arXiv:2603.25209
- URL: https://arxiv.org/abs/2603.25209
- Category: S3 Positional / RoPE memory 
- Priority: Related
- Training-free: Yes
- Original status: From prior docs / To verify
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

FreeLOC 属于 S3 Positional / RoPE memory；当前根据题名、分类表与可用网页元信息定位为：作为非因果/位置 remap 背景，注意与因果 AR 差异。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 relative position remapping 在 VRPR + temporal sparse attention 载体上的建模、压缩、检索或评测。

## 4. Memory object

- relative position remapping

## 5. Memory substrate

- VRPR + temporal sparse attention

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 relative position remapping 相关的历史状态或评测对象。 |
| Store | 主要载体：VRPR + temporal sparse attention。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 作为非因果/位置 remap 背景，注意与因果 AR 差异。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S3 Positional / RoPE memory 
- Role: 作为非因果/位置 remap 背景，注意与因果 AR 差异。
- Priority: Related


---

# Infinity-RoPE

## 1. Metadata

- Year: 2025/2026
- ID: arXiv:2511.20649
- URL: https://arxiv.org/abs/2511.20649
- Category: S3 Positional / RoPE memory 
- Priority: High
- Training-free: Inference
- Original status: Verified by web
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

Infinity-RoPE 属于 S3 Positional / RoPE memory；当前根据题名、分类表与可用网页元信息定位为：action-controllable infinite video 的位置策略。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 infinite rollout coordinates 在 Block-Relativistic RoPE / KV Flush / RoPE Cut 载体上的建模、压缩、检索或评测。

## 4. Memory object

- infinite rollout coordinates

## 5. Memory substrate

- Block-Relativistic RoPE / KV Flush / RoPE Cut

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 infinite rollout coordinates 相关的历史状态或评测对象。 |
| Store | 主要载体：Block-Relativistic RoPE / KV Flush / RoPE Cut。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 action-controllable infinite video 的位置策略。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S3 Positional / RoPE memory 
- Role: action-controllable infinite video 的位置策略。
- Priority: High


---

# MemRoPE

## 1. Metadata

- Year: 2026
- ID: arXiv:2603.12513
- URL: https://arxiv.org/abs/2603.12513
- Category: S3 Positional / RoPE memory 
- Priority: Must-read
- Training-free: Yes
- Original status: Verified by web
- Reading status: web-read
- Source basis: arXiv abstract web-read

## 2. One-sentence takeaway

MemRoPE co-designs content compression and temporal coordinates: long/short EMA memory tokens only make sense if keys are cached before RoPE and positional embeddings are applied online.

## 3. Problem / failure mode

Sliding-window caches discard past context, causing fidelity degradation, identity drift, and motion stagnation; static attention sinks cannot reflect evolving video content.

## 4. Memory object

- position-free historical keys

## 5. Memory substrate

- unrotated key cache + online RoPE indexing

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | Continuously writes all past keys into dual long-term and short-term EMA streams. |
| Store | Fixed-size memory tokens plus unrotated key cache. |
| Retrieve | Attention retrieves aggregated long/short memory with RoPE applied dynamically at attention time. |
| Use | Provides global identity/history and recent dynamics within fixed cache for unbounded generation. |
| Update | EMA updates long-term and short-term memory streams as video grows. |
| Forget | Forgetting is implicit through EMA decay rather than explicit semantic conflict detection. |
| Evaluate | Minute- to hour-scale generation with temporal coherence, visual fidelity and subject consistency. |

## 7. Strengths for this survey

Key paper for positional memory: content memory cannot be separated from coordinate design.

## 8. Limitations / second-pass PDF checks

EMA memory is still implicit and does not know which entity or attribute is being stored.

## 9. Recommended placement

- Main category: S3 Positional / RoPE memory 
- Role: 位置相位冲突的最重要代表。
- Priority: Must-read


---

# Pyramid Forcing

## 1. Metadata

- Year: 2026
- ID: arXiv:2605.13111
- URL: https://arxiv.org/abs/2605.13111
- Category: S3 Positional / RoPE memory 
- Priority: Must-read
- Training-free: Yes
- Original status: Verified by web
- Reading status: web-read
- Source basis: arXiv abstract web-read

## 2. One-sentence takeaway

Pyramid Forcing makes attention-head heterogeneity central: Anchor, Wave, and Veil heads have different temporal dependencies and therefore need different KV policies.

## 3. Problem / failure mode

Unified historical-frame retention assumes homogeneous dependencies across attention heads and causes long-term degradation.

## 4. Memory object

- long-horizon temporal coordinates

## 5. Memory substrate

- dynamic RoPE remap + head-aware cache

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | Profiles attention heads offline and writes history into heterogeneous head-aware cache structures. |
| Store | Ragged/pyramidal KV cache with behavior-specific cache lengths and policies. |
| Retrieve | Anchor heads access broad context; Wave heads retrieve periodic dependencies; Veil heads focus on initial/adjacent frames. |
| Use | Improves long-horizon generation quality on Self Forcing and Causal Forcing. |
| Update | Cache is updated according to head type rather than global FIFO policy. |
| Forget | Implicit through per-head cache policy and retention budgets. |
| Evaluate | VBench-Long; reported 60s Self Forcing score improvement from 77.87 to 81.21. |

## 7. Strengths for this survey

Core reference for head/layer-specialized memory and a natural precursor to identity/motion/layout head routing.

## 8. Limitations / second-pass PDF checks

Head types are temporal roles, not explicit semantic roles such as identity/entity/layout.

## 9. Recommended placement

- Main category: S3 Positional / RoPE memory 
- Role: 将位置 remap 和 head-aware cache 结合。
- Priority: Must-read


---

# RIFLEx / UltraViCo / LoL: Longer-than-Longer

## 1. Metadata

- Year: 2025/2026
- ID: various
- URL: N/A
- Category: S3 Positional / RoPE memory 
- Priority: Related
- Training-free: Yes
- Original status: From prior docs / To verify individually
- Reading status: metadata-only
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

RIFLEx / UltraViCo / LoL: Longer-than-Longer 属于 S3 Positional / RoPE memory；当前根据题名、分类表与可用网页元信息定位为：作为位置外推和 sink/phase 相关参考。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 RoPE extrapolation / long-context video 在 frequency/phase adjustment 载体上的建模、压缩、检索或评测。

## 4. Memory object

- RoPE extrapolation / long-context video

## 5. Memory substrate

- frequency/phase adjustment

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 RoPE extrapolation / long-context video 相关的历史状态或评测对象。 |
| Store | 主要载体：frequency/phase adjustment。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 作为位置外推和 sink/phase 相关参考。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S3 Positional / RoPE memory 
- Role: 作为位置外推和 sink/phase 相关参考。
- Priority: Related


---

# ConsisID: Identity-Preserving Text-to-Video Generation by Frequency Decomposition

## 1. Metadata

- Year: 2024/2025
- ID: arXiv:2411.17440
- URL: https://arxiv.org/abs/2411.17440
- Category: S4 Frequency / spectrum memory 
- Priority: High
- Training-free: Tuning-free / model-specific
- Original status: Verified by web
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

ConsisID: Identity-Preserving Text-to-Video Generation by Frequency Decomposition 属于 S4 Frequency / spectrum memory；当前根据题名、分类表与可用网页元信息定位为：把身份保持和频率分解联系起来。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 face identity details 在 low/high-frequency identity injection 载体上的建模、压缩、检索或评测。

## 4. Memory object

- face identity details

## 5. Memory substrate

- low/high-frequency identity injection

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 face identity details 相关的历史状态或评测对象。 |
| Store | 主要载体：low/high-frequency identity injection。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 把身份保持和频率分解联系起来。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S4 Frequency / spectrum memory 
- Role: 把身份保持和频率分解联系起来。
- Priority: High


---

# FLEX

## 1. Metadata

- Year: 2026
- ID: arXiv:2602.14027
- URL: https://arxiv.org/abs/2602.14027
- Category: S4 Frequency / spectrum memory 
- Priority: High
- Training-free: Yes
- Original status: Verified by web
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

FLEX 属于 S4 Frequency / spectrum memory；当前根据题名、分类表与可用网页元信息定位为：连接 positional memory 和 spectral memory。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 dynamic prior and temporal frequency 在 frequency-aware RoPE + antiphase noise 载体上的建模、压缩、检索或评测。

## 4. Memory object

- dynamic prior and temporal frequency

## 5. Memory substrate

- frequency-aware RoPE + antiphase noise

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 dynamic prior and temporal frequency 相关的历史状态或评测对象。 |
| Store | 主要载体：frequency-aware RoPE + antiphase noise。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 连接 positional memory 和 spectral memory。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S4 Frequency / spectrum memory 
- Role: 连接 positional memory 和 spectral memory。
- Priority: High


---

# FreeLong / FreeLong++: Training-Free Long Video Generation via Multi-band SpectralFusion

## 1. Metadata

- Year: 2025
- ID: arXiv:2507.00162
- URL: https://arxiv.org/abs/2507.00162
- Category: S4 Frequency / spectrum memory 
- Priority: High
- Training-free: Yes
- Original status: Verified by web
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

FreeLong / FreeLong++: Training-Free Long Video Generation via Multi-band SpectralFusion 属于 S4 Frequency / spectrum memory；当前根据题名、分类表与可用网页元信息定位为：频域长视频方法主代表。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 long-range semantics + high-frequency details 在 multi-band spectral fusion / 3D FFT / SpecMix 载体上的建模、压缩、检索或评测。

## 4. Memory object

- long-range semantics + high-frequency details

## 5. Memory substrate

- multi-band spectral fusion / 3D FFT / SpecMix

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 long-range semantics + high-frequency details 相关的历史状态或评测对象。 |
| Store | 主要载体：multi-band spectral fusion / 3D FFT / SpecMix。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 频域长视频方法主代表。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S4 Frequency / spectrum memory 
- Role: 频域长视频方法主代表。
- Priority: High


---

# FreeSpec: Training-Free Long Video Generation via Singular-Spectrum Reconstruction

## 1. Metadata

- Year: 2026
- ID: arXiv:2605.06509
- URL: https://arxiv.org/abs/2605.06509
- Category: S4 Frequency / spectrum memory 
- Priority: High
- Training-free: Yes
- Original status: Verified by web
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

FreeSpec: Training-Free Long Video Generation via Singular-Spectrum Reconstruction 属于 S4 Frequency / spectrum memory；当前根据题名、分类表与可用网页元信息定位为：解释大窗口 attention 的低秩谱集中和动态抹平。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 dynamic/high-frequency preservation 在 singular-spectrum reconstruction 载体上的建模、压缩、检索或评测。

## 4. Memory object

- dynamic/high-frequency preservation

## 5. Memory substrate

- singular-spectrum reconstruction

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 dynamic/high-frequency preservation 相关的历史状态或评测对象。 |
| Store | 主要载体：singular-spectrum reconstruction。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 解释大窗口 attention 的低秩谱集中和动态抹平。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S4 Frequency / spectrum memory 
- Role: 解释大窗口 attention 的低秩谱集中和动态抹平。
- Priority: High


---

# Frequency Regulation for Video Generation

## 1. Metadata

- Year: 2025
- ID: ACM MM 2025?
- URL: N/A
- Category: S4 Frequency / spectrum memory 
- Priority: Related
- Training-free: Likely yes/inference
- Original status: From prior docs / To verify
- Reading status: metadata-only
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

Frequency Regulation for Video Generation 属于 S4 Frequency / spectrum memory；当前根据题名、分类表与可用网页元信息定位为：作为频域修正背景。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 spectral degeneration / motion detail 在 frequency-domain noise/feature regulation 载体上的建模、压缩、检索或评测。

## 4. Memory object

- spectral degeneration / motion detail

## 5. Memory substrate

- frequency-domain noise/feature regulation

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 spectral degeneration / motion detail 相关的历史状态或评测对象。 |
| Store | 主要载体：frequency-domain noise/feature regulation。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 作为频域修正背景。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S4 Frequency / spectrum memory 
- Role: 作为频域修正背景。
- Priority: Related


---

# Closed-Loop Triplet Synergistic Generation for Long-Form Video / CoTriSyGen

## 1. Metadata

- Year: 2026
- ID: arXiv:2606.16184
- URL: https://arxiv.org/abs/2606.16184
- Category: S5 Identity / entity / narrative memory 
- Priority: High
- Training-free: Agentic pipeline
- Original status: Verified by web, added in v0.2
- Reading status: web-read
- Source basis: arXiv/search abstract web-read

## 2. One-sentence takeaway

CoTriSyGen treats long-video generation as a closed-loop visual-text-memory synergy, where generated visuals update future prompts and mutable entity memory.

## 3. Problem / failure mode

Storyboard pipelines are often feed-forward and cannot feed generated evidence back into later conditioning, causing cross-shot identity and composition inconsistencies.

## 4. Memory object

- entity/narrative memory

## 5. Memory substrate

- closed-loop visual-text-memory synergy + mutable visual state

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | Writes newly manifested/evolved entities, multi-view evidence, and generated visual evidence into mutable visual state. |
| Store | Entity-centric mutable visual-state memory. |
| Retrieve | VLM analyzer reasons over planned intent, persistent memory and generated visuals. |
| Use | Intra-shot regeneration/refinement and inter-shot prompt rewriting propagate entity attributes and cinematic continuity. |
| Update | Closed-loop analyzer updates prompts and memory after observing generated results. |
| Forget | Not the focus; conflict resolution/update policy should be checked in PDF. |
| Evaluate | StoryBench; cross-shot consistency, prompt adherence and cinematic continuity. |

## 7. Strengths for this survey

Strong narrative-memory example for Update/closed-loop memory lifecycle.

## 8. Limitations / second-pass PDF checks

Agentic/VLM loop may be slow and complex; not a low-level KV method.

## 9. Recommended placement

- Main category: S5 Identity / entity / narrative memory 
- Role: 补充 narrative memory：把生成结果反馈到 memory/prompt，体现 memory update 生命周期。
- Priority: High


---

# ConsisID

## 1. Metadata

- Year: 2024/2025
- ID: arXiv:2411.17440
- URL: https://arxiv.org/abs/2411.17440
- Category: S5 Identity / entity / narrative memory 
- Priority: High
- Training-free: Tuning-free / model-specific
- Original status: Verified by web
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

ConsisID 属于 S5 Identity / entity / narrative memory；当前根据题名、分类表与可用网页元信息定位为：reference ID + frequency memory 双重相关。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 human identity 在 frequency-decomposed reference identity features 载体上的建模、压缩、检索或评测。

## 4. Memory object

- human identity

## 5. Memory substrate

- frequency-decomposed reference identity features

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 human identity 相关的历史状态或评测对象。 |
| Store | 主要载体：frequency-decomposed reference identity features。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 reference ID + frequency memory 双重相关。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S5 Identity / entity / narrative memory 
- Role: reference ID + frequency memory 双重相关。
- Priority: High


---

# Corgi: Cached Memory Guided Video Generation

## 1. Metadata

- Year: 2025
- ID: WACV 2025
- URL: https://openaccess.thecvf.com/content/WACV2025/papers/Wu_Corgi_Cached_Memory_Guided_Video_Generation_WACV_2025_paper.pdf
- Category: S5 Identity / entity / narrative memory 
- Priority: High
- Training-free: Trained/system
- Original status: Verified by web
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

Corgi: Cached Memory Guided Video Generation 属于 S5 Identity / entity / narrative memory；当前根据题名、分类表与可用网页元信息定位为：早期 cached memory-guided video generation 代表。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 multi-scene core memories 在 cached latent memory bank / key frames 载体上的建模、压缩、检索或评测。

## 4. Memory object

- multi-scene core memories

## 5. Memory substrate

- cached latent memory bank / key frames

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 multi-scene core memories 相关的历史状态或评测对象。 |
| Store | 主要载体：cached latent memory bank / key frames。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 早期 cached memory-guided video generation 代表。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S5 Identity / entity / narrative memory 
- Role: 早期 cached memory-guided video generation 代表。
- Priority: High


---

# EM-Vid: Training-Free Entity-Centric Memory for Efficient and Consistent Multi-Shot Video Generation

## 1. Metadata

- Year: 2026
- ID: arXiv:2605.23610
- URL: https://arxiv.org/abs/2605.23610
- Category: S5 Identity / entity / narrative memory 
- Priority: Must-read
- Training-free: Yes
- Original status: Verified by web
- Reading status: web-read
- Source basis: arXiv abstract web-read

## 2. One-sentence takeaway

EM-Vid argues full-frame memory entangles persistent entity information with transient scene context; it stores entity-indexed latent patches and conditions sparsely on entity-relevant tokens.

## 3. Problem / failure mode

Multi-shot videos need recurring entities to stay consistent while following shot-specific prompts; reusing full frames leaks irrelevant context and is costly.

## 4. Memory object

- recurring entity appearance

## 5. Memory substrate

- entity-indexed sparse latent patch bank

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | Writes entity-relevant latent patches into an entity-indexed memory bank. |
| Store | Sparse latent patch bank keyed by entity. |
| Retrieve | Sparse token conditioning restricts attention to entity-relevant tokens. |
| Use | Improves subject consistency, prompt adherence and efficiency in multi-shot generation. |
| Update | Budgeted memory update maintains compact evolving memory. |
| Forget | Budgeted update likely evicts less useful entity evidence; exact policy needs PDF. |
| Evaluate | Multi-shot script format; evaluates prompt adherence, efficiency and subject consistency. |

## 7. Strengths for this survey

Clean argument for why memory units should be entity patches, not full frames.

## 8. Limitations / second-pass PDF checks

Requires reliable entity indexing and may need PDF for exact patch selection/noise injection details.

## 9. Recommended placement

- Main category: S5 Identity / entity / narrative memory 
- Role: entity-centric latent patch memory 的核心代表。
- Priority: Must-read


---

# Gloria: Consistent Character Video Generation via Content Anchors

## 1. Metadata

- Year: 2026
- ID: arXiv:2603.29931
- URL: https://arxiv.org/abs/2603.29931
- Category: S5 Identity / entity / narrative memory 
- Priority: High
- Training-free: Likely trained/system
- Original status: Verified by web
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

Gloria: Consistent Character Video Generation via Content Anchors 属于 S5 Identity / entity / narrative memory；当前根据题名、分类表与可用网页元信息定位为：anchor-based character memory 代表。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 character identity across long videos 在 character-centric/content anchor frames 载体上的建模、压缩、检索或评测。

## 4. Memory object

- character identity across long videos

## 5. Memory substrate

- character-centric/content anchor frames

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 character identity across long videos 相关的历史状态或评测对象。 |
| Store | 主要载体：character-centric/content anchor frames。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 anchor-based character memory 代表。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S5 Identity / entity / narrative memory 
- Role: anchor-based character memory 代表。
- Priority: High


---

# IAMFlow: Advancing Narrative Long Video Generation via Training-Free Identity-Aware Memory

## 1. Metadata

- Year: 2026
- ID: arXiv:2605.18733
- URL: https://arxiv.org/abs/2605.18733
- Category: S5 Identity / entity / narrative memory 
- Priority: Must-read
- Training-free: Yes
- Original status: Verified by web
- Reading status: web-read
- Source basis: arXiv abstract web-read

## 2. One-sentence takeaway

IAMFlow turns narrative video memory from implicit similarity matching into explicit global entity tracking with LLM-extracted IDs and VLM-verified attributes.

## 3. Problem / failure mode

Prompt transitions and shifting references cause identity drift, character duplication and attribute loss when history is compressed or retrieved only with coarse implicit attention.

## 4. Memory object

- entity identity and attributes

## 5. Memory substrate

- LLM global ID table + VLM attribute verification

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | LLM extracts entities and visual attributes from each prompt; generated frames are asynchronously verified by a VLM. |
| Store | Global ID table and identity-aware memory with attribute records. |
| Retrieve | Entity/global-ID matching retrieves identity memory rather than relying on temporal or visual similarity alone. |
| Use | Guides generation across prompt transitions and narrative scripts. |
| Update | VLM verification refines attributes from rendered frames; adaptive prompt transition updates conditioning. |
| Forget | Not mainly a forgetting paper; likely handles changes via attribute update rather than cache decay. |
| Evaluate | Introduces NarraStream-Bench with 324 multi-prompt scripts and MLLM-assisted evaluation. |

## 7. Strengths for this survey

Best representative of explicit identity/entity memory for narrative generation.

## 8. Limitations / second-pass PDF checks

LLM/VLM pipeline is heavier than pure attention/KV intervention and may depend on parser/verifier reliability.

## 9. Recommended placement

- Main category: S5 Identity / entity / narrative memory 
- Role: 显式实体/身份记忆核心论文。
- Priority: Must-read


---

# LaVieID / ConsistI2V / Concat-ID / Identity-GRPO / FantasyID

## 1. Metadata

- Year: 2024-2026
- ID: various
- URL: N/A
- Category: S5 Identity / entity / narrative memory 
- Priority: Related baseline
- Training-free: mixed
- Original status: From prior docs / To verify individually
- Reading status: metadata-only
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

LaVieID / ConsistI2V / Concat-ID / Identity-GRPO / FantasyID 属于 S5 Identity / entity / narrative memory；当前根据题名、分类表与可用网页元信息定位为：ID 保持 related work，需逐篇复核后决定是否纳入主表。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 reference ID preservation 在 ID adapter / local ID routing / RLHF / reference features 载体上的建模、压缩、检索或评测。

## 4. Memory object

- reference ID preservation

## 5. Memory substrate

- ID adapter / local ID routing / RLHF / reference features

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 reference ID preservation 相关的历史状态或评测对象。 |
| Store | 主要载体：ID adapter / local ID routing / RLHF / reference features。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 ID 保持 related work，需逐篇复核后决定是否纳入主表。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S5 Identity / entity / narrative memory 
- Role: ID 保持 related work，需逐篇复核后决定是否纳入主表。
- Priority: Related baseline


---

# Lynx

## 1. Metadata

- Year: 2025
- ID: arXiv:2509.15496
- URL: https://arxiv.org/abs/2509.15496
- Category: S5 Identity / entity / narrative memory 
- Priority: Related baseline
- Training-free: Trained adapter
- Original status: From prior docs / To verify
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

Lynx 属于 S5 Identity / entity / narrative memory；当前根据题名、分类表与可用网页元信息定位为：如果综述涉及 ID baseline，可作为 Wan2.1 ID 保持训练式代表。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 Wan2.1 reference identity 在 ArcFace ID adapter + reference adapter + discriminator 载体上的建模、压缩、检索或评测。

## 4. Memory object

- Wan2.1 reference identity

## 5. Memory substrate

- ArcFace ID adapter + reference adapter + discriminator

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 Wan2.1 reference identity 相关的历史状态或评测对象。 |
| Store | 主要载体：ArcFace ID adapter + reference adapter + discriminator。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 如果综述涉及 ID baseline，可作为 Wan2.1 ID 保持训练式代表。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S5 Identity / entity / narrative memory 
- Role: 如果综述涉及 ID baseline，可作为 Wan2.1 ID 保持训练式代表。
- Priority: Related baseline


---

# Memento: Reconstruct to Remember for Consistent Long Video Generation

## 1. Metadata

- Year: 2026
- ID: arXiv:2606.14667
- URL: https://arxiv.org/abs/2606.14667
- Category: S5 Identity / entity / narrative memory 
- Priority: Must-read
- Training-free: Trained/system
- Original status: Verified by web
- Reading status: web-read
- Source basis: arXiv/search abstract web-read

## 2. One-sentence takeaway

Memento makes an important diagnostic claim: if a memory bank really preserves a subject, it should be able to reconstruct that subject from memory alone.

## 3. Problem / failure mode

Shot-by-shot long-video methods optimize plausible next shots but do not verify whether historical memory still contains identity-critical subject evidence.

## 4. Memory object

- recurring subject identity

## 5. Memory substrate

- subject-reconstruction-guided memory bank + dual-query memory

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | Writes historical subject evidence and global story captions into a memory bank supervised by reconstruction. |
| Store | Historical memory bank plus dual-query mechanism for identity evidence and short-context keyframes. |
| Retrieve | One query retrieves identity-relevant long-range memory; another selects short-context keyframes for continuation. |
| Use | Jointly trains next-shot generation and memory-based subject reconstruction. |
| Update | Memory evolves as story proceeds; reconstruction supervision preserves identity evidence. |
| Forget | Aims to prevent dilution/overwriting/forgetting but exact forgetting mechanism needs PDF. |
| Evaluate | Long-term subject consistency, cross-shot coherence and visual quality. |

## 7. Strengths for this survey

Excellent for evaluation section: reconstruction tests whether memory is actually useful.

## 8. Limitations / second-pass PDF checks

Requires training/data pipeline and precise subject descriptions; less plug-and-play than training-free methods.

## 9. Recommended placement

- Main category: S5 Identity / entity / narrative memory 
- Role: 把“记住主体”显式转化为可从 memory 重建主体的监督目标。
- Priority: Must-read


---

# Memory-V2V: Augmenting Video-to-Video Diffusion Models with Memory

## 1. Metadata

- Year: 2026
- ID: arXiv:2601.16296
- URL: https://arxiv.org/abs/2601.16296
- Category: S5 Identity / entity / narrative memory 
- Priority: High
- Training-free: System / possible trained compressor
- Original status: Verified by web
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

Memory-V2V: Augmenting Video-to-Video Diffusion Models with Memory 属于 S5 Identity / entity / narrative memory；当前根据题名、分类表与可用网页元信息定位为：V2V 多轮编辑 memory 代表。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 multi-turn editing consistency 在 external cache + retrieval + dynamic tokenization + compressor 载体上的建模、压缩、检索或评测。

## 4. Memory object

- multi-turn editing consistency

## 5. Memory substrate

- external cache + retrieval + dynamic tokenization + compressor

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 multi-turn editing consistency 相关的历史状态或评测对象。 |
| Store | 主要载体：external cache + retrieval + dynamic tokenization + compressor。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 V2V 多轮编辑 memory 代表。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S5 Identity / entity / narrative memory 
- Role: V2V 多轮编辑 memory 代表。
- Priority: High


---

# SlotMemory: Object-Centric KV Memory for Streaming Long-Video Generation

## 1. Metadata

- Year: 2026
- ID: arXiv:2605.31033
- URL: https://arxiv.org/abs/2605.31033
- Category: S5 Identity / entity / narrative memory 
- Priority: Must-read
- Training-free: Likely yes/system
- Original status: Verified by web
- Reading status: web-read
- Source basis: arXiv abstract web-read

## 2. One-sentence takeaway

SlotMemory shifts memory abstraction from when something happened to what entity is represented, decomposing KV memory into reusable semantic slots.

## 3. Problem / failure mode

Temporal-centric memory organized as frames/chunks/unclustered tokens causes identity drift and semantic inconsistency when entities leave the frame or prompts change.

## 4. Memory object

- object/entity persistence

## 5. Memory substrate

- object-centric KV semantic slots

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | Stores high-fidelity key-value tokens into discrete semantic slots. |
| Store | Object-centric KV slots used as routing addresses. |
| Retrieve | Prompt-aware retrieval addresses semantic slots rather than time windows. |
| Use | Maintains entity-level persistence across long horizons in streaming video diffusion. |
| Update | Slots are reused/updated as entities persist or reappear. |
| Forget | Not highlighted in abstract; budget and slot replacement policy should be checked in PDF. |
| Evaluate | 60-second interactive narratives with Wan2.1-T2V-1.3B; reports SOTA score and dynamic consistency improvement. |

## 7. Strengths for this survey

Key bridge between KV memory and entity memory.

## 8. Limitations / second-pass PDF checks

Need PDF for slot construction, routing loss/algorithm and ablations.

## 9. Recommended placement

- Main category: S5 Identity / entity / narrative memory 
- Role: 从 temporal-centric memory 转向 object-centric memory 的核心代表。
- Priority: Must-read


---

# StoryDiffusion: Consistent Self-Attention for Long-Range Image and Video Generation

## 1. Metadata

- Year: 2024
- ID: arXiv:2405.01434
- URL: https://arxiv.org/abs/2405.01434
- Category: S5 Identity / entity / narrative memory 
- Priority: High
- Training-free: Mostly training-free / system
- Original status: Verified by web
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

StoryDiffusion: Consistent Self-Attention for Long-Range Image and Video Generation 属于 S5 Identity / entity / narrative memory；当前根据题名、分类表与可用网页元信息定位为：早期 consistent attention / character consistency 代表。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 story character identity 在 consistent self-attention + semantic motion predictor 载体上的建模、压缩、检索或评测。

## 4. Memory object

- story character identity

## 5. Memory substrate

- consistent self-attention + semantic motion predictor

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 story character identity 相关的历史状态或评测对象。 |
| Store | 主要载体：consistent self-attention + semantic motion predictor。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 早期 consistent attention / character consistency 代表。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S5 Identity / entity / narrative memory 
- Role: 早期 consistent attention / character consistency 代表。
- Priority: High


---

# StoryMem: Long Story Video Generation with Compact Memory Bank

## 1. Metadata

- Year: 2025
- ID: arXiv:2512.19539
- URL: https://arxiv.org/html/2512.19539v1
- Category: S5 Identity / entity / narrative memory 
- Priority: High
- Training-free: Trained/system
- Original status: Verified by web
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

StoryMem: Long Story Video Generation with Compact Memory Bank 属于 S5 Identity / entity / narrative memory；当前根据题名、分类表与可用网页元信息定位为：故事视频中的 compact memory bank。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 characters / scenes / style 在 compact memory bank + shot-by-shot generation 载体上的建模、压缩、检索或评测。

## 4. Memory object

- characters / scenes / style

## 5. Memory substrate

- compact memory bank + shot-by-shot generation

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 characters / scenes / style 相关的历史状态或评测对象。 |
| Store | 主要载体：compact memory bank + shot-by-shot generation。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 故事视频中的 compact memory bank。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S5 Identity / entity / narrative memory 
- Role: 故事视频中的 compact memory bank。
- Priority: High


---

# TPIGE: Identity-Preserving Text-to-Video Generation via Training-Free Prompt, Image, and Guidance Enhancement

## 1. Metadata

- Year: 2025
- ID: arXiv:2509.01362
- URL: https://arxiv.org/abs/2509.01362
- Category: S5 Identity / entity / narrative memory 
- Priority: High
- Training-free: Yes
- Original status: Verified by web
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

TPIGE: Identity-Preserving Text-to-Video Generation via Training-Free Prompt, Image, and Guidance Enhancement 属于 S5 Identity / entity / narrative memory；当前根据题名、分类表与可用网页元信息定位为：输入侧 training-free ID memory 的代表。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 reference subject identity 在 prompt/image/guidance enhancement 载体上的建模、压缩、检索或评测。

## 4. Memory object

- reference subject identity

## 5. Memory substrate

- prompt/image/guidance enhancement

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 reference subject identity 相关的历史状态或评测对象。 |
| Store | 主要载体：prompt/image/guidance enhancement。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 输入侧 training-free ID memory 的代表。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S5 Identity / entity / narrative memory 
- Role: 输入侧 training-free ID memory 的代表。
- Priority: High


---

# Video Storyboarding: Multi-Shot Character Consistency for Text-to-Video Generation

## 1. Metadata

- Year: 2024
- ID: arXiv:2412.07750
- URL: https://arxiv.org/abs/2412.07750
- Category: S5 Identity / entity / narrative memory 
- Priority: Must-read
- Training-free: Yes
- Original status: Verified by web
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

Video Storyboarding: Multi-Shot Character Consistency for Text-to-Video Generation 属于 S5 Identity / entity / narrative memory；当前根据题名、分类表与可用网页元信息定位为：identity-motion trade-off 的关键论文。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 multi-shot character identity 在 query injection / attention feature control 载体上的建模、压缩、检索或评测。

## 4. Memory object

- multi-shot character identity

## 5. Memory substrate

- query injection / attention feature control

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 multi-shot character identity 相关的历史状态或评测对象。 |
| Store | 主要载体：query injection / attention feature control。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 identity-motion trade-off 的关键论文。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S5 Identity / entity / narrative memory 
- Role: identity-motion trade-off 的关键论文。
- Priority: Must-read


---

# VideoMemory: Toward Consistent Video Generation via Memory Integration

## 1. Metadata

- Year: 2026
- ID: arXiv:2601.03655
- URL: https://arxiv.org/abs/2601.03655
- Category: S5 Identity / entity / narrative memory 
- Priority: Must-read
- Training-free: Trained/system
- Original status: Verified by web
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

VideoMemory: Toward Consistent Video Generation via Memory Integration 属于 S5 Identity / entity / narrative memory；当前根据题名、分类表与可用网页元信息定位为：显式 memory integration 论文，适合 narrative memory 章节。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 character / prop / background consistency 在 dynamic memory bank 载体上的建模、压缩、检索或评测。

## 4. Memory object

- character / prop / background consistency

## 5. Memory substrate

- dynamic memory bank

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 character / prop / background consistency 相关的历史状态或评测对象。 |
| Store | 主要载体：dynamic memory bank。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 显式 memory integration 论文，适合 narrative memory 章节。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S5 Identity / entity / narrative memory 
- Role: 显式 memory integration 论文，适合 narrative memory 章节。
- Priority: Must-read


---

# Context as Memory: Scene-Consistent Interactive Long Video Generation with Memory Retrieval

## 1. Metadata

- Year: 2025
- ID: arXiv:2506.03141
- URL: https://arxiv.org/abs/2506.03141
- Category: S6 Retrieval / external memory 
- Priority: High
- Training-free: System / likely training-light
- Original status: Verified by web
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

Context as Memory: Scene-Consistent Interactive Long Video Generation with Memory Retrieval 属于 S6 Retrieval / external memory；当前根据题名、分类表与可用网页元信息定位为：历史帧检索作为 memory 的核心代表。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 scene revisit / context visibility 在 historical context frames as memory 载体上的建模、压缩、检索或评测。

## 4. Memory object

- scene revisit / context visibility

## 5. Memory substrate

- historical context frames as memory

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 scene revisit / context visibility 相关的历史状态或评测对象。 |
| Store | 主要载体：historical context frames as memory。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 历史帧检索作为 memory 的核心代表。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S6 Retrieval / external memory 
- Role: 历史帧检索作为 memory 的核心代表。
- Priority: High


---

# LongLive-RAG

## 1. Metadata

- Year: 2026
- ID: arXiv:2606.02553
- URL: https://arxiv.org/abs/2606.02553
- Category: S6 Retrieval / external memory 
- Priority: High
- Training-free: System
- Original status: Verified by web
- Reading status: web-read
- Source basis: arXiv/search abstract web-read

## 2. One-sentence takeaway

LongLive-RAG is the cleanest RAG formulation for AR video generation: previously generated latents become a content-addressable searchable history, not just a sliding window.

## 3. Problem / failure mode

Sliding-window attention creates irreversible error accumulation because later blocks condition only on degraded recent trajectory.

## 4. Memory object

- long-range context retrieval

## 5. Memory substrate

- retrieval-augmented historical frames/tokens

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | Writes self-generated latents into dynamic searchable history. |
| Store | Content-addressable latent memory indexed by query embeddings. |
| Retrieve | At each new block, retrieves relevant historical latents; Window Temporal Delta Loss makes embeddings capture meaningful temporal changes. |
| Use | Conditions generator on non-local historical context to reduce drift. |
| Update | History grows with generated latents; retrieval index updates dynamically. |
| Forget | Not central; retrieval rather than permanent retention handles relevance. |
| Evaluate | Multiple AR backbones/lengths and VBench-Long rank. |

## 7. Strengths for this survey

Core S6 retrieval-memory paper for video generation.

## 8. Limitations / second-pass PDF checks

Retrieval quality depends on embedding/indexing; not explicitly entity-aware.

## 9. Recommended placement

- Main category: S6 Retrieval / external memory 
- Role: RAG for long video generation。
- Priority: High


---

# MAG

## 1. Metadata

- Year: 2025
- ID: arXiv:2512.18741
- URL: https://arxiv.org/html/2512.18741v1
- Category: S6 Retrieval / external memory 
- Priority: High
- Training-free: Trained/system
- Original status: Verified by web
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

MAG 属于 S6 Retrieval / external memory；当前根据题名、分类表与可用网页元信息定位为：memory compression/retrieval 章节。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 historical scene consistency 在 decoupled memory compression and generation 载体上的建模、压缩、检索或评测。

## 4. Memory object

- historical scene consistency

## 5. Memory substrate

- decoupled memory compression and generation

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 historical scene consistency 相关的历史状态或评测对象。 |
| Store | 主要载体：decoupled memory compression and generation。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 memory compression/retrieval 章节。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S6 Retrieval / external memory 
- Role: memory compression/retrieval 章节。
- Priority: High


---

# MALT Diffusion

## 1. Metadata

- Year: 2025/2026
- ID: OpenReview
- URL: https://openreview.net/forum?id=YuUwwVoWES
- Category: S6 Retrieval / external memory 
- Priority: Related
- Training-free: Trained
- Original status: From prior docs / To verify
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

MALT Diffusion 属于 S6 Retrieval / external memory；当前根据题名、分类表与可用网页元信息定位为：latent transformer memory 背景。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 long video latent memory 在 memory-augmented latent transformer 载体上的建模、压缩、检索或评测。

## 4. Memory object

- long video latent memory

## 5. Memory substrate

- memory-augmented latent transformer

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 long video latent memory 相关的历史状态或评测对象。 |
| Store | 主要载体：memory-augmented latent transformer。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 latent transformer memory 背景。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S6 Retrieval / external memory 
- Role: latent transformer memory 背景。
- Priority: Related


---

# MemCam

## 1. Metadata

- Year: 2026
- ID: arXiv:2603.26193
- URL: https://arxiv.org/abs/2603.26193
- Category: S6 Retrieval / external memory 
- Priority: Related
- Training-free: System
- Original status: From prior docs / To verify
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

MemCam 属于 S6 Retrieval / external memory；当前根据题名、分类表与可用网页元信息定位为：camera/FOV-aware retrieval memory。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 camera-controlled scene consistency 在 compressed memory frames + co-visibility retrieval 载体上的建模、压缩、检索或评测。

## 4. Memory object

- camera-controlled scene consistency

## 5. Memory substrate

- compressed memory frames + co-visibility retrieval

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 camera-controlled scene consistency 相关的历史状态或评测对象。 |
| Store | 主要载体：compressed memory frames + co-visibility retrieval。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 camera/FOV-aware retrieval memory。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S6 Retrieval / external memory 
- Role: camera/FOV-aware retrieval memory。
- Priority: Related


---

# SlotMemory / EM-Vid / DecMem

## 1. Metadata

- Year: 2026
- ID: arXiv:2605.31033
- URL: https://arxiv.org/abs/2605.31033
- Category: S6 Retrieval / external memory 
- Priority: High
- Training-free: mixed
- Original status: Verified for SlotMemory and EM-Vid; DecMem to verify
- Reading status: web-read
- Source basis: arXiv abstract web-read

## 2. One-sentence takeaway

SlotMemory shifts memory abstraction from when something happened to what entity is represented, decomposing KV memory into reusable semantic slots.

## 3. Problem / failure mode

Temporal-centric memory organized as frames/chunks/unclustered tokens causes identity drift and semantic inconsistency when entities leave the frame or prompts change.

## 4. Memory object

- entity/global-local memory

## 5. Memory substrate

- slot / patch bank / decoupled memory

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | Stores high-fidelity key-value tokens into discrete semantic slots. |
| Store | Object-centric KV slots used as routing addresses. |
| Retrieve | Prompt-aware retrieval addresses semantic slots rather than time windows. |
| Use | Maintains entity-level persistence across long horizons in streaming video diffusion. |
| Update | Slots are reused/updated as entities persist or reappear. |
| Forget | Not highlighted in abstract; budget and slot replacement policy should be checked in PDF. |
| Evaluate | 60-second interactive narratives with Wan2.1-T2V-1.3B; reports SOTA score and dynamic consistency improvement. |

## 7. Strengths for this survey

Key bridge between KV memory and entity memory.

## 8. Limitations / second-pass PDF checks

Need PDF for slot construction, routing loss/algorithm and ablations.

## 9. Recommended placement

- Main category: S6 Retrieval / external memory 
- Role: 作为 retrieval memory 与 entity memory 的交叉。
- Priority: High


---

# SWIFT

## 1. Metadata

- Year: 2026
- ID: arXiv:2605.09442
- URL: https://arxiv.org/html/2605.09442v1
- Category: S6 Retrieval / external memory 
- Priority: Related
- Training-free: Likely yes
- Original status: From prior docs / To verify
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

SWIFT 属于 S6 Retrieval / external memory；当前根据题名、分类表与可用网页元信息定位为：prompt-adaptive external/internal memory。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 multi-prompt semantic memory 在 semantic injection cache + adaptive window 载体上的建模、压缩、检索或评测。

## 4. Memory object

- multi-prompt semantic memory

## 5. Memory substrate

- semantic injection cache + adaptive window

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 multi-prompt semantic memory 相关的历史状态或评测对象。 |
| Store | 主要载体：semantic injection cache + adaptive window。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 prompt-adaptive external/internal memory。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S6 Retrieval / external memory 
- Role: prompt-adaptive external/internal memory。
- Priority: Related


---

# Beyond Pixel Histories: World Models with Persistent 3D Memory

## 1. Metadata

- Year: 2026
- ID: arXiv:2603.03482
- URL: https://arxiv.org/abs/2603.03482
- Category: S7 Video world model memory 
- Priority: High
- Training-free: Trained/system
- Original status: Verified by web
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

Beyond Pixel Histories: World Models with Persistent 3D Memory 属于 S7 Video world model memory；当前根据题名、分类表与可用网页元信息定位为：强调 pixel history 不足，需要 persistent 3D memory。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 persistent 3D environment 在 explicit 3D memory beyond pixel histories 载体上的建模、压缩、检索或评测。

## 4. Memory object

- persistent 3D environment

## 5. Memory substrate

- explicit 3D memory beyond pixel histories

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 persistent 3D environment 相关的历史状态或评测对象。 |
| Store | 主要载体：explicit 3D memory beyond pixel histories。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 强调 pixel history 不足，需要 persistent 3D memory。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S7 Video world model memory 
- Role: 强调 pixel history 不足，需要 persistent 3D memory。
- Priority: High


---

# DriveWAM: Video Generative Priors Enable Scalable World-Action Modeling for Autonomous Driving

## 1. Metadata

- Year: 2026
- ID: arXiv:2605.28544
- URL: https://arxiv.org/abs/2605.28544
- Category: S7 Video world model memory 
- Priority: Related
- Training-free: Trained
- Original status: Verified by web
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

DriveWAM: Video Generative Priors Enable Scalable World-Action Modeling for Autonomous Driving 属于 S7 Video world model memory；当前根据题名、分类表与可用网页元信息定位为：自动驾驶 world-action model 背景。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 driving world-action memory 在 video-action generation / autonomous driving world model 载体上的建模、压缩、检索或评测。

## 4. Memory object

- driving world-action memory

## 5. Memory substrate

- video-action generation / autonomous driving world model

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 driving world-action memory 相关的历史状态或评测对象。 |
| Store | 主要载体：video-action generation / autonomous driving world model。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 自动驾驶 world-action model 背景。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S7 Video world model memory 
- Role: 自动驾驶 world-action model 背景。
- Priority: Related


---

# Echo-Memory: Action World Model Memory Benchmark / Study

## 1. Metadata

- Year: 2026
- ID: arXiv:2606.09803
- URL: https://arxiv.org/abs/2606.09803
- Category: S7 Video world model memory 
- Priority: Related
- Training-free: N/A
- Original status: Verified by web
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

Echo-Memory: Action World Model Memory Benchmark / Study 属于 S7 Video world model memory；当前根据题名、分类表与可用网页元信息定位为：action world model memory 评测/分析。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 action world model memory 在 benchmark/study 载体上的建模、压缩、检索或评测。

## 4. Memory object

- action world model memory

## 5. Memory substrate

- benchmark/study

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 action world model memory 相关的历史状态或评测对象。 |
| Store | 主要载体：benchmark/study。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 action world model memory 评测/分析。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S7 Video world model memory 
- Role: action world model memory 评测/分析。
- Priority: Related


---

# GAIA-1 / DriveDreamer / MagicDrive / Vista / Copilot4D / DriveLM etc.

## 1. Metadata

- Year: 2023-2025
- ID: various
- URL: N/A
- Category: S7 Video world model memory 
- Priority: Background
- Training-free: Trained
- Original status: Need targeted verification if included
- Reading status: metadata-only
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

GAIA-1 / DriveDreamer / MagicDrive / Vista / Copilot4D / DriveLM etc. 属于 S7 Video world model memory；当前根据题名、分类表与可用网页元信息定位为：不是 memory 主线，但可作为 autonomous-driving world model 背景。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 driving world simulation / scene dynamics 在 world model / controllable driving video 载体上的建模、压缩、检索或评测。

## 4. Memory object

- driving world simulation / scene dynamics

## 5. Memory substrate

- world model / controllable driving video

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 driving world simulation / scene dynamics 相关的历史状态或评测对象。 |
| Store | 主要载体：world model / controllable driving video。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 不是 memory 主线，但可作为 autonomous-driving world model 背景。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S7 Video world model memory 
- Role: 不是 memory 主线，但可作为 autonomous-driving world model 背景。
- Priority: Background


---

# Genie 2: A Large-scale Foundation World Model

## 1. Metadata

- Year: 2024
- ID: DeepMind blog
- URL: https://deepmind.google/blog/genie-2-a-large-scale-foundation-world-model/
- Category: S7 Video world model memory 
- Priority: Background
- Training-free: N/A
- Original status: Verified by web
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

Genie 2: A Large-scale Foundation World Model 属于 S7 Video world model memory；当前根据题名、分类表与可用网页元信息定位为：用于工业界 world memory 叙事。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 long-horizon world memory 在 foundation world model / interactive environment 载体上的建模、压缩、检索或评测。

## 4. Memory object

- long-horizon world memory

## 5. Memory substrate

- foundation world model / interactive environment

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 long-horizon world memory 相关的历史状态或评测对象。 |
| Store | 主要载体：foundation world model / interactive environment。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 用于工业界 world memory 叙事。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S7 Video world model memory 
- Role: 用于工业界 world memory 叙事。
- Priority: Background


---

# Genie 3: A New Frontier for World Models

## 1. Metadata

- Year: 2025
- ID: DeepMind blog
- URL: https://deepmind.google/blog/genie-3-a-new-frontier-for-world-models/
- Category: S7 Video world model memory 
- Priority: Background
- Training-free: N/A
- Original status: Verified by web
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

Genie 3: A New Frontier for World Models 属于 S7 Video world model memory；当前根据题名、分类表与可用网页元信息定位为：说明实时交互与长期一致性是 world model 核心能力。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 real-time interactive world memory 在 general-purpose real-time world model 载体上的建模、压缩、检索或评测。

## 4. Memory object

- real-time interactive world memory

## 5. Memory substrate

- general-purpose real-time world model

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 real-time interactive world memory 相关的历史状态或评测对象。 |
| Store | 主要载体：general-purpose real-time world model。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 说明实时交互与长期一致性是 world model 核心能力。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S7 Video world model memory 
- Role: 说明实时交互与长期一致性是 world model 核心能力。
- Priority: Background


---

# Hybrid Spatial Memory / MosaicMem

## 1. Metadata

- Year: 2026
- ID: arXiv:2603.17117
- URL: https://arxiv.org/abs/2603.17117
- Category: S7 Video world model memory 
- Priority: High
- Training-free: Trained/system
- Original status: Verified by web
- Reading status: web-read
- Source basis: arXiv abstract web-read

## 2. One-sentence takeaway

MosaicMem combines explicit 3D patch lifting with native generative conditioning, preserving stable spatial evidence while allowing dynamic regions to evolve.

## 3. Problem / failure mode

Explicit 3D structures improve reprojection consistency but struggle with moving objects; implicit memory can have inaccurate camera motion.

## 4. Memory object

- controllable spatial world model

## 5. Memory substrate

- 3D patch lifting + native conditioning

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | Lifts patches into 3D for localization and targeted retrieval. |
| Store | Hybrid spatial memory of spatially aligned patches plus model-native conditioning. |
| Retrieve | Patch-and-compose interface retrieves patches in queried view. |
| Use | Preserves persistent scene components while inpainting what should evolve; supports minute-level navigation and scene editing. |
| Update | Memory alignment methods and autoregressive rollout update spatial memory. |
| Forget | Not a forgetting paper; evolving regions are delegated to model inpainting rather than stored rigidly. |
| Evaluate | Pose adherence, dynamic modeling, navigation, editing and rollout. |

## 7. Strengths for this survey

Excellent contrast to Mirage: hybrid explicit/implicit spatial memory.

## 8. Limitations / second-pass PDF checks

Complex spatial alignment and pose conditioning.

## 9. Recommended placement

- Main category: S7 Video world model memory 
- Role: hybrid spatial memory。
- Priority: High


---

# Latent Spatial Memory for Video World Models / Mirage

## 1. Metadata

- Year: 2026
- ID: arXiv:2606.09828
- URL: https://arxiv.org/abs/2606.09828
- Category: S7 Video world model memory 
- Priority: Must-read
- Training-free: Trained/system
- Original status: Verified by web
- Reading status: web-read
- Source basis: arXiv abstract web-read

## 2. One-sentence takeaway

Mirage argues spatial memory should live in diffusion latent space, not pixel-space point clouds, to avoid repeated rendering/VAE encoding and feature loss.

## 3. Problem / failure mode

Explicit RGB point-cloud memory is expensive and lossy for 3D-consistent video world models.

## 4. Memory object

- 3D spatial persistence

## 5. Memory substrate

- persistent latent 3D cache / depth-guided back-projection

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | Lifts diffusion latent tokens into 3D via depth-guided back-projection. |
| Store | Persistent 3D cache in diffusion latent space. |
| Retrieve | Queries memory by latent-space warping to synthesize novel views. |
| Use | Eliminates pixel-space reconstruction bottlenecks and improves WorldScore/RealEstate10K reconstruction. |
| Update | Latent cache is updated as new scene regions are generated/observed. |
| Forget | Not central; memory footprint reduction is emphasized. |
| Evaluate | Reports up to 10.57x faster end-to-end generation and 55x memory footprint reduction vs explicit 3D baselines. |

## 7. Strengths for this survey

Key example of substrate innovation: latent 3D memory.

## 8. Limitations / second-pass PDF checks

Needs depth/geometric pipeline; PDF needed for exact warping and cache updates.

## 9. Recommended placement

- Main category: S7 Video world model memory 
- Role: latent-space 3D memory 核心新论文。
- Priority: Must-read


---

# Learning World Models for Interactive Video Generation

## 1. Metadata

- Year: 2025
- ID: arXiv:2505.21996
- URL: https://arxiv.org/abs/2505.21996
- Category: S7 Video world model memory 
- Priority: High
- Training-free: Trained/system
- Original status: Verified by web
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

Learning World Models for Interactive Video Generation 属于 S7 Video world model memory；当前根据题名、分类表与可用网页元信息定位为：指出 AR world model 的 compounding error 和 insufficient memory mechanisms。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 interactive rollout memory 在 VRAG + explicit global state conditioning 载体上的建模、压缩、检索或评测。

## 4. Memory object

- interactive rollout memory

## 5. Memory substrate

- VRAG + explicit global state conditioning

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 interactive rollout memory 相关的历史状态或评测对象。 |
| Store | 主要载体：VRAG + explicit global state conditioning。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 指出 AR world model 的 compounding error 和 insufficient memory mechanisms。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S7 Video world model memory 
- Role: 指出 AR world model 的 compounding error 和 insufficient memory mechanisms。
- Priority: High


---

# LiveWorld: Simulating Out-of-Sight Dynamics in Generative Video World Models

## 1. Metadata

- Year: 2026
- ID: arXiv:2603.07145
- URL: https://arxiv.org/abs/2603.07145
- Category: S7 Video world model memory 
- Priority: Must-read
- Training-free: Trained/system
- Original status: Verified by web
- Reading status: web-read
- Source basis: arXiv abstract web-read

## 2. One-sentence takeaway

LiveWorld formalizes out-of-sight dynamics: objects should continue evolving when unobserved, not freeze in observational memory.

## 3. Problem / failure mode

Existing video world models implicitly assume the world evolves only inside the field of view; revisits fail to reflect unseen events.

## 4. Memory object

- out-of-sight dynamic world state

## 5. Memory substrate

- persistent global state: static 3D background + dynamic entities

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | Writes static 3D background and dynamic entities into persistent global state. |
| Store | Persistent global state with static/dynamic decomposition. |
| Retrieve | On revisit, synchronizes evolved dynamic entity state for spatially coherent rendering. |
| Use | Monitor-based mechanism simulates temporal progression of active entities while unobserved. |
| Update | Dynamic entities continue evolving autonomously and are synchronized on revisit. |
| Forget | Not central; persistent evolution rather than forgetting. |
| Evaluate | Introduces LiveBench for maintaining out-of-sight dynamics. |

## 7. Strengths for this survey

Most important paper for world-state memory and hidden-state evolution.

## 8. Limitations / second-pass PDF checks

World-model-specific; not directly a T2V identity baseline.

## 9. Recommended placement

- Main category: S7 Video world model memory 
- Role: out-of-sight dynamics 核心论文：世界不可见时也应继续演化。
- Priority: Must-read


---

# Long-Context State-Space Video World Models

## 1. Metadata

- Year: 2025
- ID: arXiv:2505.20171
- URL: https://arxiv.org/abs/2505.20171
- Category: S7 Video world model memory 
- Priority: High
- Training-free: Trained
- Original status: Verified by web
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

Long-Context State-Space Video World Models 属于 S7 Video world model memory；当前根据题名、分类表与可用网页元信息定位为：state-space memory 替代 attention memory 的代表。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 long-range world state 在 SSM global memory + local attention 载体上的建模、压缩、检索或评测。

## 4. Memory object

- long-range world state

## 5. Memory substrate

- SSM global memory + local attention

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 long-range world state 相关的历史状态或评测对象。 |
| Store | 主要载体：SSM global memory + local attention。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 state-space memory 替代 attention memory 的代表。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S7 Video world model memory 
- Role: state-space memory 替代 attention memory 的代表。
- Priority: High


---

# MIND: Benchmarking Memory Consistency and Action Control in World Models

## 1. Metadata

- Year: 2026
- ID: arXiv:2602.08025
- URL: https://arxiv.org/abs/2602.08025
- Category: S7 Video world model memory 
- Priority: Must-read
- Training-free: N/A
- Original status: Verified by web
- Reading status: web-read
- Source basis: arXiv abstract web-read

## 2. One-sentence takeaway

MIND is a benchmark rather than generation method; it explicitly evaluates memory consistency and action control in open-domain closed-loop revisits.

## 3. Problem / failure mode

World models lack a unified benchmark for remembering and predicting dynamic visual environments under control.

## 4. Memory object

- memory consistency + action control

## 5. Memory substrate

- benchmark / closed-loop revisiting

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | N/A method; benchmark constructs closed-loop revisited evaluation settings. |
| Store | N/A; evaluates whether model memory is consistent across viewpoints/actions. |
| Retrieve | N/A; tests whether model recalls context under revisits. |
| Use | Used to evaluate world models and MIND-World baseline. |
| Update | N/A benchmark. |
| Forget | N/A benchmark; reveals forgetting failures. |
| Evaluate | 250 high-quality 1080p 24FPS videos; first-person/third-person clips, shared and varied action spaces; memory consistency and action control. |

## 7. Strengths for this survey

Essential evaluation reference for world-model memory.

## 8. Limitations / second-pass PDF checks

Focuses world models, not standard T2V/I2V.

## 9. Recommended placement

- Main category: S7 Video world model memory 
- Role: world model memory 评测核心 benchmark。
- Priority: Must-read


---

# MoVerse

## 1. Metadata

- Year: 2026
- ID: arXiv:2606.13376
- URL: https://arxiv.org/abs/2606.13376
- Category: S7 Video world model memory 
- Priority: Related
- Training-free: Trained/system
- Original status: Verified by web
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

MoVerse 属于 S7 Video world model memory；当前根据题名、分类表与可用网页元信息定位为：单图到可导航世界中的 spatial memory。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 interactive navigable world from image 在 spatial memory + Gaussian-conditioned rendering 载体上的建模、压缩、检索或评测。

## 4. Memory object

- interactive navigable world from image

## 5. Memory substrate

- spatial memory + Gaussian-conditioned rendering

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 interactive navigable world from image 相关的历史状态或评测对象。 |
| Store | 主要载体：spatial memory + Gaussian-conditioned rendering。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 单图到可导航世界中的 spatial memory。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S7 Video world model memory 
- Role: 单图到可导航世界中的 spatial memory。
- Priority: Related


---

# RELIC: Real-time Long Context Interactive World Models

## 1. Metadata

- Year: 2025
- ID: arXiv:2512.04040
- URL: https://arxiv.org/abs/2512.04040
- Category: S7 Video world model memory 
- Priority: Must-read
- Training-free: Trained/system
- Original status: Verified by web
- Reading status: web-read
- Source basis: arXiv abstract web-read

## 2. One-sentence takeaway

RELIC unifies real-time long-horizon streaming, spatial memory and action control by encoding compressed historical latent tokens with relative actions and absolute camera poses inside the KV cache.

## 3. Problem / failure mode

Interactive world models need real-time streaming, spatial memory and precise control simultaneously, but long-term memory often hurts performance.

## 4. Memory object

- interactive long-context memory

## 5. Memory substrate

- compressed historical latent tokens + actions + camera poses in KV cache

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | Writes historical latent tokens along with relative actions and absolute camera poses. |
| Store | Compact camera-aware latent tokens in KV cache. |
| Retrieve | Implicit 3D-consistent retrieval from camera-aware memory. |
| Use | Enables memory-aware scene exploration from a single image and text description. |
| Update | Causal student/self-forcing paradigm supports long rollouts from teacher distillation. |
| Forget | Not central; compression is primary memory-budget mechanism. |
| Evaluate | 16 FPS, action following, long-horizon streaming and spatial-memory retrieval on Unreal Engine-rendered data. |

## 7. Strengths for this survey

Best example of KV memory becoming action/camera-aware world memory.

## 8. Limitations / second-pass PDF checks

Large 14B trained system; harder to compare directly with training-free video generators.

## 9. Recommended placement

- Main category: S7 Video world model memory 
- Role: 实时交互 world model 中 latent/history/action/camera 统一进 KV cache。
- Priority: Must-read


---

# ReMind: Dynamic Memory for Out-of-Sight State Evolution

## 1. Metadata

- Year: 2026
- ID: arXiv:2605.25333
- URL: https://arxiv.org/html/2605.25333v1
- Category: S7 Video world model memory 
- Priority: High
- Training-free: Trained/system
- Original status: Verified by web
- Reading status: web-read
- Source basis: arXiv abstract web-read

## 2. One-sentence takeaway

ReMind teaches video generators to use KV cache as dynamic memory through memory-oriented data, event-aware training and cache adaptation.

## 3. Problem / failure mode

Video generators freeze hidden states after interruptions because they are rarely trained to use non-local KV retrieval as dynamic memory.

## 4. Memory object

- hidden state evolution

## 5. Memory substrate

- memory-oriented data + event-aware training + cache adaptation

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | Constructs camera-annotated frame graphs with protected anchors, degraded intervals and temporal gaps. |
| Store | KV cache/pathways adapted for dynamic memory; PM-RoPE supports camera-phase retrieval. |
| Retrieve | Node-structured curriculum forces retrieval of relevant past states across interruptions. |
| Use | Supports frontier continuation and reference-cache training for out-of-sight state evolution. |
| Update | Event-aware training and cache adaptation induce dynamic memory behavior. |
| Forget | Uses noisy memory/node-drop/degraded intervals to robustify retrieval under missing/dirty memory. |
| Evaluate | STEVO-Bench and recovery tasks; also checks general I2V to avoid catastrophic forgetting. |

## 7. Strengths for this survey

Important training-data/curriculum counterpart to LiveWorld.

## 8. Limitations / second-pass PDF checks

Requires new data/training rather than training-free inference.

## 9. Recommended placement

- Main category: S7 Video world model memory 
- Role: out-of-sight state evolution。
- Priority: High


---

# Video World Models with Long-term Spatial Memory / SpMem

## 1. Metadata

- Year: 2025
- ID: arXiv:2506.05284
- URL: https://arxiv.org/abs/2506.05284
- Category: S7 Video world model memory 
- Priority: Must-read
- Training-free: Trained/system
- Original status: Verified by web
- Reading status: web-read
- Source basis: arXiv abstract web-read

## 2. One-sentence takeaway

SpMem frames world-model memory after human memory: working memory for recent context, geometry-grounded long-term spatial memory for revisits, and episodic memory for past experiences.

## 3. Problem / failure mode

Action-conditioned world models forget previously generated environments during revisits because temporal context windows are limited.

## 4. Memory object

- long-term spatial memory

## 5. Memory substrate

- working memory + spatial memory + episodic memory

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | Writes generated/observed scene information into explicitly stored 3D memory mechanisms. |
| Store | Working memory, geometry-grounded long-term spatial memory and episodic memory. |
| Retrieve | Retrieves spatial information for scene revisits and long-term consistency. |
| Use | Improves quality, consistency and context length of video world models. |
| Update | Memory is updated as the model explores/generates new parts of the scene. |
| Forget | Not explicit; focus on long-term storage/retrieval. |
| Evaluate | Custom datasets for training/evaluating explicitly stored 3D memory mechanisms. |

## 7. Strengths for this survey

Key taxonomy paper for working/spatial/episodic world-model memory.

## 8. Limitations / second-pass PDF checks

Requires geometry grounding and custom data.

## 9. Recommended placement

- Main category: S7 Video world model memory 
- Role: geometry-grounded spatial memory 核心论文。
- Priority: Must-read


---

# WorldMem: Long-term Consistent World Simulation with Memory

## 1. Metadata

- Year: 2025
- ID: arXiv:2504.12369
- URL: https://arxiv.org/abs/2504.12369
- Category: S7 Video world model memory 
- Priority: Must-read
- Training-free: Trained/system
- Original status: Verified by web
- Reading status: web-read
- Source basis: arXiv abstract web-read

## 2. One-sentence takeaway

WorldMem stores memory frames together with states such as poses and timestamps, making world memory state-conditioned rather than raw-frame-only.

## 3. Problem / failure mode

Limited temporal context causes failures in long-term consistency, especially 3D spatial consistency.

## 4. Memory object

- scene/world memory

## 5. Memory substrate

- memory frames + states such as pose/timestamp

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | Writes memory frames and their states (pose, timestamp) into a memory bank. |
| Store | Memory units combining visual frames and state metadata. |
| Retrieve | Memory attention extracts relevant memory frames based on states. |
| Use | Reconstructs previously observed scenes under viewpoint/temporal gaps and supports dynamic evolution via timestamps. |
| Update | Memory bank grows/updates as world is observed over time. |
| Forget | Not emphasized; memory selection/retrieval is more central. |
| Evaluate | Virtual and real scenarios; spatial reconstruction and interaction. |

## 7. Strengths for this survey

Canonical world-model memory paper: memory must include state metadata.

## 8. Limitations / second-pass PDF checks

Frame/state bank can be heavier than latent memory; PDF needed for details.

## 9. Recommended placement

- Main category: S7 Video world model memory 
- Role: 视频世界模型 memory bank 代表。
- Priority: Must-read


---

# WorldPack: Compressed Memory Improves Spatial Consistency in Video World Modeling

## 1. Metadata

- Year: 2025
- ID: arXiv:2512.02473
- URL: https://arxiv.org/abs/2512.02473
- Category: S7 Video world model memory 
- Priority: High
- Training-free: Trained/system
- Original status: Verified by web
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

WorldPack: Compressed Memory Improves Spatial Consistency in Video World Modeling 属于 S7 Video world model memory；当前根据题名、分类表与可用网页元信息定位为：压缩记忆提升空间一致性。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 spatial consistency 在 trajectory packing + memory retrieval 载体上的建模、压缩、检索或评测。

## 4. Memory object

- spatial consistency

## 5. Memory substrate

- trajectory packing + memory retrieval

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 spatial consistency 相关的历史状态或评测对象。 |
| Store | 主要载体：trajectory packing + memory retrieval。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 压缩记忆提升空间一致性。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S7 Video world model memory 
- Role: 压缩记忆提升空间一致性。
- Priority: High


---

# WorldPlay

## 1. Metadata

- Year: 2025
- ID: arXiv:2512.14614
- URL: https://arxiv.org/abs/2512.14614
- Category: S7 Video world model memory 
- Priority: High
- Training-free: Trained/system
- Original status: Verified by web
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

WorldPlay 属于 S7 Video world model memory；当前根据题名、分类表与可用网页元信息定位为：interactive world modeling 中动态重建过去上下文。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 real-time interaction / memory attenuation 在 Reconstituted Context Memory 载体上的建模、压缩、检索或评测。

## 4. Memory object

- real-time interaction / memory attenuation

## 5. Memory substrate

- Reconstituted Context Memory

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 real-time interaction / memory attenuation 相关的历史状态或评测对象。 |
| Store | 主要载体：Reconstituted Context Memory。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 interactive world modeling 中动态重建过去上下文。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S7 Video world model memory 
- Role: interactive world modeling 中动态重建过去上下文。
- Priority: High


---

# LiveBench / LoopNav / MAG-Bench

## 1. Metadata

- Year: 2025-2026
- ID: various
- URL: N/A
- Category: S8 Evaluation / benchmarks 
- Priority: Related
- Training-free: N/A
- Original status: From prior docs / To verify
- Reading status: metadata-only
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

LiveBench / LoopNav / MAG-Bench 属于 S8 Evaluation / benchmarks；当前根据题名、分类表与可用网页元信息定位为：作为未来补充评测集合，需逐个验证。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 revisit / loop / interactive consistency 在 benchmarks 载体上的建模、压缩、检索或评测。

## 4. Memory object

- revisit / loop / interactive consistency

## 5. Memory substrate

- benchmarks

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 revisit / loop / interactive consistency 相关的历史状态或评测对象。 |
| Store | 主要载体：benchmarks。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 作为未来补充评测集合，需逐个验证。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S8 Evaluation / benchmarks 
- Role: 作为未来补充评测集合，需逐个验证。
- Priority: Related


---

# MIND

## 1. Metadata

- Year: 2026
- ID: arXiv:2602.08025
- URL: https://arxiv.org/abs/2602.08025
- Category: S8 Evaluation / benchmarks 
- Priority: Must cite
- Training-free: N/A
- Original status: Verified by web
- Reading status: web-read
- Source basis: arXiv abstract web-read

## 2. One-sentence takeaway

MIND is a benchmark rather than generation method; it explicitly evaluates memory consistency and action control in open-domain closed-loop revisits.

## 3. Problem / failure mode

World models lack a unified benchmark for remembering and predicting dynamic visual environments under control.

## 4. Memory object

- world model memory consistency and action control

## 5. Memory substrate

- closed-loop revisiting benchmark

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | N/A method; benchmark constructs closed-loop revisited evaluation settings. |
| Store | N/A; evaluates whether model memory is consistent across viewpoints/actions. |
| Retrieve | N/A; tests whether model recalls context under revisits. |
| Use | Used to evaluate world models and MIND-World baseline. |
| Update | N/A benchmark. |
| Forget | N/A benchmark; reveals forgetting failures. |
| Evaluate | 250 high-quality 1080p 24FPS videos; first-person/third-person clips, shared and varied action spaces; memory consistency and action control. |

## 7. Strengths for this survey

Essential evaluation reference for world-model memory.

## 8. Limitations / second-pass PDF checks

Focuses world models, not standard T2V/I2V.

## 9. Recommended placement

- Main category: S8 Evaluation / benchmarks 
- Role: world-state memory 评测核心。
- Priority: Must cite


---

# NarraStream-Bench

## 1. Metadata

- Year: 2026
- ID: arXiv:2605.18733
- URL: https://arxiv.org/abs/2605.18733
- Category: S8 Evaluation / benchmarks 
- Priority: High
- Training-free: N/A
- Original status: Verified by web
- Reading status: web-read
- Source basis: arXiv abstract web-read

## 2. One-sentence takeaway

IAMFlow turns narrative video memory from implicit similarity matching into explicit global entity tracking with LLM-extracted IDs and VLM-verified attributes.

## 3. Problem / failure mode

Prompt transitions and shifting references cause identity drift, character duplication and attribute loss when history is compressed or retrieved only with coarse implicit attention.

## 4. Memory object

- multi-prompt narrative identity consistency

## 5. Memory substrate

- benchmark

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | LLM extracts entities and visual attributes from each prompt; generated frames are asynchronously verified by a VLM. |
| Store | Global ID table and identity-aware memory with attribute records. |
| Retrieve | Entity/global-ID matching retrieves identity memory rather than relying on temporal or visual similarity alone. |
| Use | Guides generation across prompt transitions and narrative scripts. |
| Update | VLM verification refines attributes from rendered frames; adaptive prompt transition updates conditioning. |
| Forget | Not mainly a forgetting paper; likely handles changes via attribute update rather than cache decay. |
| Evaluate | Introduces NarraStream-Bench with 324 multi-prompt scripts and MLLM-assisted evaluation. |

## 7. Strengths for this survey

Best representative of explicit identity/entity memory for narrative generation.

## 8. Limitations / second-pass PDF checks

LLM/VLM pipeline is heavier than pure attention/KV intervention and may depend on parser/verifier reliability.

## 9. Recommended placement

- Main category: S8 Evaluation / benchmarks 
- Role: 叙事/多实体 identity memory 评测。
- Priority: High


---

# VBench / VBench-Long

## 1. Metadata

- Year: 2023-2025
- ID: benchmark
- URL: https://vchitect.github.io/VBench-project/
- Category: S8 Evaluation / benchmarks 
- Priority: Must cite
- Training-free: N/A
- Original status: From prior docs / To verify
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

VBench / VBench-Long 属于 S8 Evaluation / benchmarks；当前根据题名、分类表与可用网页元信息定位为：长视频质量和一致性常用指标；需指出会被 loop/frozen video 误导。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 video quality, subject consistency, dynamic degree 在 automatic benchmark 载体上的建模、压缩、检索或评测。

## 4. Memory object

- video quality, subject consistency, dynamic degree

## 5. Memory substrate

- automatic benchmark

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 video quality, subject consistency, dynamic degree 相关的历史状态或评测对象。 |
| Store | 主要载体：automatic benchmark。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 长视频质量和一致性常用指标；需指出会被 loop/frozen video 误导。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S8 Evaluation / benchmarks 
- Role: 长视频质量和一致性常用指标；需指出会被 loop/frozen video 误导。
- Priority: Must cite


---

# VQeval

## 1. Metadata

- Year: 2026
- ID: arXiv:2605.31057
- URL: https://arxiv.org/abs/2605.31057
- Category: S8 Evaluation / benchmarks 
- Priority: High
- Training-free: N/A
- Original status: Verified by web
- Reading status: metadata-web-ready
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

VQeval 属于 S8 Evaluation / benchmarks；当前根据题名、分类表与可用网页元信息定位为：评测章节的重要论据：普通 consistency metric 可能奖励静止循环。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 loop/frozen degeneration penalty 在 metric/evaluator 载体上的建模、压缩、检索或评测。

## 4. Memory object

- loop/frozen degeneration penalty

## 5. Memory substrate

- metric/evaluator

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 loop/frozen degeneration penalty 相关的历史状态或评测对象。 |
| Store | 主要载体：metric/evaluator。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 评测章节的重要论据：普通 consistency metric 可能奖励静止循环。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S8 Evaluation / benchmarks 
- Role: 评测章节的重要论据：普通 consistency metric 可能奖励静止循环。
- Priority: High


---

# WorldScore

## 1. Metadata

- Year: 2025/2026
- ID: benchmark
- URL: N/A
- Category: S8 Evaluation / benchmarks 
- Priority: Related
- Training-free: N/A
- Original status: From web abstract / To verify
- Reading status: metadata-only
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

WorldScore 属于 S8 Evaluation / benchmarks；当前根据题名、分类表与可用网页元信息定位为：Mirage 等 world model memory 论文中使用。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 world model spatial/3D quality 在 benchmark 载体上的建模、压缩、检索或评测。

## 4. Memory object

- world model spatial/3D quality

## 5. Memory substrate

- benchmark

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 world model spatial/3D quality 相关的历史状态或评测对象。 |
| Store | 主要载体：benchmark。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 Mirage 等 world model memory 论文中使用。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S8 Evaluation / benchmarks 
- Role: Mirage 等 world model memory 论文中使用。
- Priority: Related

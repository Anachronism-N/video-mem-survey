# WEB_READ_INDEX_v0.5_SUPPLEMENT

> Supplementary web-read notes for new candidates collected after v0.4.  
> Stage: discussion / candidate validation, not final inclusion.

## New or resolved candidates

- MAGI-1 — S1 AR/streaming backbone.
- StreamingT2V — S1/S5 early explicit video memory precursor.
- SkyReels-V2 — S1 long-form system / film generation background.
- Pyramid Flow — S1 long-video backbone background.
- Sparse VideoGen — S2 sparse attention and head-role background.
- Sparse Forcing — S2 implicit spatiotemporal KV memory candidate.
- RIFLEx — S3 positional frequency / length extrapolation.
- LoL: Longer than Longer — S3/S2 sink-collapse and RoPE-head conflict.
- AnyID — S5 universal identity-preserving video generation.
- Slot-ID — S5 temporal identity dynamics from reference videos.
- OpenAI Sora technical report — S7/video-generation-as-world-simulator background.
- V-JEPA 2 — S7 latent world-model boundary/background.
- UniDriveDreamer — S7 multimodal driving world model background.
- WorldScore — S8 world generation benchmark.
- MBench — S8 memory capability benchmark for video world models.
- iWorld-Bench — S8 interactive world-model benchmark with memory tasks.

## Note file

All notes are currently consolidated in:

```text
notes/paper_notes/WEB_READ_NOTES_SUPPLEMENT_v0.5.md
```

Reason: v0.5 is still a discussion and scope-confirmation phase. After the final scope is confirmed, these notes can be split into one-file-per-paper and merged into `web_read_all_v0.4/` or a new `web_read_all_v0.5/` directory.

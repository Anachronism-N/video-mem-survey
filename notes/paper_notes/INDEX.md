# Paper Notes Index

当前版本：v0.2，生成日期 2026-06-17。

说明：本目录包含重点论文精读/结构化阅读笔记。受当前运行环境无法直接下载 PDF 的限制，部分笔记基于 arXiv abstract/HTML、已有调研文档和本轮 web 检索摘要生成；需要在本地下载 PDF 后继续补全公式、图表和实验细节。

## Tier-1 / Must-read notes

- Echo-Forcing — S2 KV cache / attention memory。
- MemRoPE — S2/S3 positional + token memory。
- Deep Forcing — S2 sink/compression memory。
- Pyramid Forcing — S2/S3 head-aware cache and RoPE remap。
- LVSA — S2 sparse attention + rotating anchors + VQeval。
- Future Forcing — S2 future-aware KV retention。
- OmniMem — S2 full-range sparse KV retrieval。
- LongLive-RAG — S2/S6 retrieval-augmented self-generated latent history。
- Video Storyboarding — S5 identity-motion trade-off。
- ConsisID — S4/S5 frequency-decomposed identity memory。
- IAMFlow — S5 explicit identity-aware global entity memory。
- SlotMemory — S5 object-centric KV memory。
- EM-Vid — S5 entity-indexed sparse latent patch memory。
- Memento — S5 subject-reconstruction-guided historical memory。
- WorldMem — S7 memory frames + state metadata。
- SpMem / Video World Models with Long-term Spatial Memory — S7 working/spatial/episodic memory。
- RELIC — S7 realtime long-context interactive world memory。
- LiveWorld — S7 out-of-sight dynamics and persistent global state。
- Mirage / Latent Spatial Memory — S7 latent 3D cache。
- MIND — S8 memory consistency and action-control benchmark。

## Added in v0.2

- KV Cache Quantization for Self-Forcing Video Generation: A 33-Method Empirical Study — S2 system memory。
- Closed-Loop Triplet Synergistic Generation for Long-Form Video / CoTriSyGen — S5 entity-narrative closed-loop memory。

## Local full bundle

The downloadable v0.2 bundle generated in this session contains 64 structured paper-note Markdown files under `notes/paper_notes/`. GitHub currently contains this index plus planning files; the full note files are included in the ZIP artifact produced by the assistant environment.

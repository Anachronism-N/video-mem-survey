# GAIA-1 / DriveDreamer / MagicDrive / Vista / Copilot4D / DriveLM etc.

## 1. Metadata

- Year: 2023-2025
- ID: various
- URL: N/A
- Category: S7 Video world model memory 
- Priority: Background
- Training-free: Trained
- Original status: Need targeted verification if included
- Reading status: metadata-only
- Source basis: structured metadata + prior survey table

## 2. One-sentence takeaway

GAIA-1 / DriveDreamer / MagicDrive / Vista / Copilot4D / DriveLM etc. 属于 S7 Video world model memory；当前根据题名、分类表与可用网页元信息定位为：不是 memory 主线，但可作为 autonomous-driving world model 背景。。

## 3. Problem / failure mode

需要从论文网页/HTML 继续精化；初步问题是 driving world simulation / scene dynamics 在 world model / controllable driving video 载体上的建模、压缩、检索或评测。

## 4. Memory object

- driving world simulation / scene dynamics

## 5. Memory substrate

- world model / controllable driving video

## 6. Lifecycle extraction

| Stage | Extracted note |
|---|---|
| Write | 记录与 driving world simulation / scene dynamics 相关的历史状态或评测对象。 |
| Store | 主要载体：world model / controllable driving video。 |
| Retrieve | 若为方法论文，检查其是否通过 attention、query、entity ID、camera pose、semantic retrieval 或 benchmark protocol 触发记忆召回；若为综述/benchmark，则记录其评测/分类逻辑。 |
| Use | 用于 不是 memory 主线，但可作为 autonomous-driving world model 背景。。 |
| Update | 需要在二轮 PDF/HTML 精读时补充是否存在 EMA、VLM verification、memory write-back、state transition、cache refresh 或 closed-loop correction。 |
| Forget | 需要二轮精读确认是否存在 decay、eviction、flush、conflict-aware forgetting、budget pruning 或仅依靠 retrieval relevance。 |
| Evaluate | 需要记录 benchmark、指标、消融和是否可能被 loop/frozen video 误导。 |

## 7. Strengths for this survey

可作为综述分类表中的相关条目；是否精讲取决于 priority。

## 8. Limitations / second-pass PDF checks

当前为结构化精读初版；公式、图表编号和实验细节需要 PDF/HTML 二轮复核。

## 9. Recommended placement

- Main category: S7 Video world model memory 
- Role: 不是 memory 主线，但可作为 autonomous-driving world model 背景。
- Priority: Background
